# Implementation status

**Last updated:** 2026-08-03

Honest truth table for what exists in this repository. Prefer this page over roadmap marketing language when asking “is it built?”

Legend:

- **Implemented** — code present in tree
- **Tested** — automated tests green on the documented platform
- **Pending** — designed in docs; not built or not proven

---

## Platform foundation (Phase Zero partial)

| Capability | Implemented | Tested | Pending / notes |
|------------|:-----------:|:------:|-----------------|
| Backend package layout (`src/akunaki/{domain,application,ports,adapters/db,api,worker}`) | yes | yes (import/boot) | layers mostly empty by design |
| Python 3.13.14 pin + `requires-python >=3.13.14,<3.14` | yes | yes | 3.14 rejected after segfault observation |
| Exact dependency pins + `uv.lock` | yes | yes (`uv lock --check`) | stable-only policy |
| pydantic-settings config (`AKUNAKI_` prefix) | yes | yes | no model config surface |
| Safe local default `sqlite+libsql` URL | yes | yes | parent dir created on engine build |
| Local-only `database_url` validation (official `sqlite+libsql://` memory / path memory / relative / absolute file) | yes | yes | rejects hostname, credentials, port, query, fragment, non-dialect |
| Remote Turso auth token / connect_args | **no** | n/a | **intentionally deferred** (not wired; not credential-blocked) |
| SQLAlchemy 2 engine/session + FK pragma + busy_timeout(50ms) + file WAL once | yes | yes | StaticPool in-memory; QueuePool file-backed (pool_size=5, max_overflow=5, pool_timeout=5) |
| Declarative base + naming conventions | yes | yes | |
| Database readiness probe | yes | yes | |
| Alembic env + migrations (through OIDC login states) | yes | yes (up/down/up through `0012`; legacy job + `system.noop` backfill; head derived, not hardcoded) | full product schema pending |
| Reserved system tenant | yes | yes | migration `0024` seeds one fixed-id (`system`) tenant that owns platform-scoped rows — chiefly system-wide periodic jobs whose `jobs.tenant_id` FK must resolve even though the work belongs to no customer. `SYSTEM_TENANT_ID` is a pure domain constant; nothing user-facing is attributed to it. Idempotent insert; real-DB tested that a reconcile-sweep job's FK resolves to it |
| Connection lifecycle schema (`connections`, `connection_secrets`, `connection_health`) | yes | yes | migration `0004`; one connection per provider per tenant; provider/status vocabularies; ciphertext-only token storage; cascade delete |
| libSQL-compatible `Blob` column type | yes | yes | driver exposes no DBAPI `Binary`, so stock `LargeBinary` cannot bind — see Turso evidence note 4 |
| ORM models agree with migration (columns/FKs/indexes) | yes | yes | IDs caller-supplied TEXT; no UUIDv7 helper |
| Domain job lifecycle/failure types + ports Protocol (model-free) | yes | yes | immutable failure results; no SQLAlchemy in domain/ports; second-precision times; min 1s lease TTL |
| JobRepository atomic execution lifecycle + leader fencing | yes | yes | claim creates one attempt; complete/fail/retry/dead-letter/expiry history are fenced local short transactions |
| Idempotent enqueue (`enqueue_job`) deduped on `(tenant_id, idempotency_key)` | yes | yes | atomic `INSERT ... ON CONFLICT DO NOTHING`; NULL key always inserts; tenant-scoped; duplicate job id without a key raises. Dedupe covers **unsettled** jobs only (`ready`/`leased`), via the partial unique index `ux_jobs_live_idempotency_key`: the guarantee is one *in-flight* job per key, not one for all time. A retry returns to `ready` so it keeps holding the key; a terminal status (`succeeded`/`failed`/`cancelled`/`dead_letter`) releases it, so a periodic schedule keeps firing each interval instead of deduping forever against its own completed run. Tested per terminal status, plus the live (`leased` + retry-scheduled) cases |
| Durable attempt history + one-to-one dead-letter records | yes | yes | retry scheduling uses exact caller-provided delay; redacted failure messages capped at 500 chars |
| Leader lease owner/expiry pair + nonempty name checks | yes | yes | migration `0002` + model agreement |
| FastAPI app factory + `python -m akunaki.api` | yes | yes | |
| `GET /healthz` (service, db ready, `models_required=false`) | yes | yes | does not fabricate product health |
| `GET /readyz` (deep readiness for probes/dashboards) | yes | yes | read-only operational status: **migration head** (is the DB at the code's revision?), **queue depth** (ready/leased/dead-letter counts), and **leader presence** (does a worker hold the `core-reaper` lease?). Gates `ready`/HTTP 503 on DB-reachable **and** at-migration-head, so a deployment whose migrations haven't run reads not-ready; queue depth, leader, and the **audit-trail tail** (`events`, `last_event_at`) are **reported**, not gating (an idle queue, a between-deploys leaderless gap, or a quiet audit trail is not unready). The audit block is the reachable signal for a worker-written subsystem: `last_event_at` going stale while audited actions continue means the trail stopped being written, which no counter shows. It is an O(1) tail read on the `seq` primary key, plus the **stored verdict** of the last chain verification (`chain_intact`, `chain_checked_at`) — read, never computed here, since verifying is O(chain) and a probe must never trigger it. `chain_intact` is **null** when the check has never run: unknown and tampered must not read alike (mutation-tested). Read only when **at head**, because a DB behind head may not have those tables yet and the probe whose job is to report that must not itself fail on a missing one (mutation-tested). Never writes, so a probe can't perturb state. E2e tested incl. behind-head → 503 |
| `GET /metrics` (Prometheus exposition) | yes | yes | dependency-free registry (`akunaki.application.metrics`) rendering the **Prometheus text format** — an OTel collector scrapes it directly, so no client library enters the core install. **Unauthenticated**; router not registered unless `AKUNAKI_METRICS_ENABLED` is set (default off), `no-store`. **PHI-free by construction:** values are counts, labels are bounded tokens (provider, outcome class) with a length cap enforced at the call site, never a tenant id or health value. Families: `akunaki_jobs_settled_total{disposition}`, `..._dead_letters_total`, `..._lease_lost_total`, `akunaki_jobs_scheduled_total{job_type,result}`, `akunaki_connector_fetch_total{provider,result}`, `akunaki_worker_liveness`, `akunaki_worker_leader`. Liveness/leader are cleared in a `finally`, so a crashed loop never reads healthy. **An unset gauge now emits no sample at all** (counters keep their explicit zero): a gauge's zero is a *real reading*, and for a health signal it is usually the alarming one — the API process, which never runs the audit verifier, was publishing `akunaki_audit_chain_intact 0`, i.e. "tampering detected", giving anyone alerting on it a permanent false alarm. Absence means unmeasured; an explicit zero still renders. Mutation-tested. Each process serves its own registry; the worker is also observable via `/readyz`. Registry semantics, thread-safety under 8 concurrent writers, exposition format, and the enabled/disabled mount are tested |
| Internal debug surface (`/internal/debug/*`) | **removed** | n/a | fully superseded and **deleted**, along with `debug_queries.py` and the `AKUNAKI_DEBUG_ROUTES_ENABLED` setting (which no longer controlled anything). `latest-sleep` had already given way to `/v1/sleep`; `sync-status` is now the authenticated `GET /v1/connections`. **No route anywhere accepts a client-supplied `tenant_id`**, and the only unauthenticated routers left are ones that must be: `auth` (login), `healthz`/`readyz` (probes), `metrics` (PHI-free by construction, off by default), and `webhooks` (vendor-called, trust from the signature) |
| Security response headers + CORS | yes | yes | every response (incl. errors) carries a strict CSP (`default-src 'none'`), `nosniff`, `X-Frame-Options: DENY`, `no-referrer`, and same-origin COOP/CORP. Credentialed CORS only for the configured `cors_allowed_origins` allow-list (never `*`), methods GET/POST, headers `content-type`/`x-akunaki-csrf`/`x-request-id` (and the request id exposed back to the browser); empty allow-list = no cross-origin browser access. Tested |
| Request-id correlation + structured logging | yes | yes | an outermost middleware binds a per-request id — a **trusted** inbound `X-Request-ID` (matched against a bounded `[A-Za-z0-9._-]{1,128}` pattern, so it can't inject newlines/oversize into logs) or a fresh UUID — into a `ContextVar` for the request, and echoes it on the response. A `RequestIdFilter` injects it onto **every** log record (`-` outside a request), and the API entrypoint's JSON log format carries `request_id`, so logs across layers correlate to one request. Tested: generate / echo-trusted / reject-unsafe / reject-overlong / distinct-per-request / unbound-after |
| Phase-one vertical slice (connect → sync → normalize → read in API) | yes | yes | verified end-to-end in a real process against a live HTTP provider |
| Authenticated `/v1` product surface | partial | partial | login, `/v1/session`, `/v1/sleep`, `/v1/recovery`, `/v1/today`, `POST /v1/checkin`, `/v1/tools`, `/v1/provenance/{token}`, `/v1/connections/{provider}/authorize`+`/callback`, `GET /v1/connections`, `POST /v1/privacy/delete` + `GET /v1/privacy/delete/{id}`, `GET /v1/anomalies`, `GET /v1/workouts` + `GET /v1/workouts/{id}`, `POST /v1/connections/{id}/sync`, `POST /v1/confirmations`, `DELETE /v1/connections/{id}` (disconnect), `GET /v1/source-policies/effective` + `/decisions`, `GET /v1/metrics` + `/{metric}`, `GET /v1/trends`, `GET /v1/data-quality`, `GET /v1/recommendations`, `GET /v1/providers`, `GET /v1/me`, and now **`GET /v1/sync/status`** reachable. My label for this one was right — it does overlap `/v1/connections` — but for the wrong reason, and checking it found a live gap: **`sync_runs` had no writer at all**. The table shipped with the transport migration, with closed vocabularies for trigger and status and an index on `(connection_id, started_at)` built for exactly this read, and nothing ever inserted a row. `raw_payloads.sync_run_id` and `raw_revisions.sync_run_id` are FK columns meant to trace ingested data back to the fetch that produced it; both were **permanently NULL**, because `payload.get("sync_run_id")` read a key no enqueue ever set. The consequence: a sync attempt left no trace. `connection_health` carries a failure *counter* and the last error class, so a user could see "3 failures" but never when they happened, on which stream, or whether the latest attempt even ran — a retry that fixed itself was indistinguishable from one that never started. Runs are now opened **before** the fetch and closed in a `finally`, so an attempt that dies mid-flight leaves a visible `running` row rather than nothing (mutation-tested: dropping the `finally` fails four tests). Closing is guarded on `status = 'running'` so a retry reusing an id cannot rewrite a recorded failure into a success (mutation-tested). Failures record the **exception class name only** — never a vendor body, since the row is user-facing. Recording is additive and optional: an unwired handler syncs exactly as before, which keeps every existing sync test honest. The read is tenant-scoped (mutation-tested) and joins the provider, since a run id alone means nothing to a user. Also confirmed this turn that **`/v1/days/{date}` really is a duplicate** — `/v1/today` already accepts an arbitrary `day`, so it would serve byte-identical data at a second URL. Me was the last of my unverified "duplicate" labels and it was wrong too: `/v1/session` answers *is this cookie valid and whose is it* — session id, user id, tenant id, expiry — and says nothing about the **account**. The consequential field is `primary_timezone`, which was written on every tenant at signup and **read by nothing in the codebase**. Every day surface requires an explicit `day` precisely because the server must never guess a local health day from its own clock, yet the client had no way to learn which timezone defines that day either; this closes the loop. Documented as the tenant's **stated preference, not an engine input** — a fact's local health day is derived from that payload's own offset at normalization, so a client should use it to choose which `day` to request, never to reinterpret days already returned. The **OIDC subject is never echoed**: it is the identity credential login matches on, and putting an account-linking key in a browser response has no upside (mutation-tested — leaking it fails two tests). Email and display name are disclosed to their own owner; the security doc's rule makes them sensitive PII that must stay out of **logs and audit**, which is a different question from what an authenticated user may read about themselves. Two corrections to my own work along the way. The route's `account is None` branch is **unreachable**: `sessions` cascades from both `users` and `tenants`, and the privacy scrub deletes the tenant row, so a session can never outlive its account — the test now asserts the real behaviour (a 401 at the auth boundary) instead of the 404 I first wrote. And the `(user_id, tenant_id)` scope **survived a mutation through the route**, because a session's tenant is derived from the user row when it is issued, so the API cannot present a mismatched pair; the guard is defence-in-depth and is now tested at the repository, the only layer where its behaviour is observable. Providers answers the question that comes *before* linking — what can I connect, and what would it give me? `/v1/connections` describes what the user already has; this describes what is on offer. The load-bearing field is **`supports_recovery_score`**: the v0.1.0 gate needs sleep-target adherence plus HRV or resting HR, so linking Polar alone yields workouts and a permanently `insufficient` score, and nothing in the product said so. Google Health backfills sleep without a vitals stream, so it cannot reach the gate alone either. Only **implemented** capabilities are published: `ingestion-and-sync.md` carries a wide matrix explicitly labelled *proposed targets* (Oura SpO2, Polar swim structure, Health Connect, HealthKit), and serving it would promise data that never arrives — the same mistake `effective_policy` refuses to make. The table therefore mirrors the `_PROVIDER_STREAMS` sync dispatch, with a **drift guard** reading the *public* `sync_config_for_provider` so a provider whose backfill stream changes fails a test rather than reporting a stale answer (mutation-tested: mislabelling Polar as supplying sleep fails two tests). Only **configured** providers are listed: the authorize route returns an indistinguishable 404 for unknown and unconfigured so an unconfigured deployment cannot be probed for what it *could* link, and this surface must not be the back door around that (mutation-tested). Carries no credential material and no health values. One defensive branch was written and then deleted: I guarded against a stale revoked row losing to a live one, but `uq_connections_tenant_provider` makes two rows per provider impossible — the test failed with a UNIQUE violation and proved the guard was dead code. I had listed recommendations as a true duplicate of `/v1/today` — wrong, and this is the **third** of my own "blocked/redundant" labels to fail inspection. The engine resolves conflicts by priority within a group and records the losers with `suppressed_by`, and `TodaySurface` was **dropping that tuple on the floor**: computed every request, never carried, never served. So a user could not answer "why am I not being told to ease off?" — the honest answer is that a higher-priority rule won, not that nothing fired. `/v1/today` keeps rendering only primary/supporting (its response is unchanged, so no client breaks); this surface is the recommendation set itself **plus** the suppressed rules, each naming the rule that beat it. Mutation-tested at both layers: dropping the field in the composite and dropping it in the route's projection each fail a test, and so does refusing to project `suppressed_by` — the disclosure is worthless without the winner named. Reaching a real collision end-to-end needs a rest-band score *and* a 28-day over-load ACWR window, so the composite test drives it off genuine conflict resolution while the route test overrides the composite to pin the serialization; an earlier version of the route test looped over an empty list and **passed while proving nothing**, which the mutation caught. `day` is required rather than defaulted to the server's today, like every day surface: a local health day belongs to the tenant's timezone. Rule ids are stable codes, not sentences — the client owns the copy. I had previously called data-quality redundant with `/v1/today`'s `data_gaps` — wrong: a gap is a property of **one day** that resolves when data arrives, while a finding is a **standing** condition the user can act on (reconnect a provider, or know one has been silent for days). Six codes ship in a closed vocabulary (a client renders copy per code, so an invented one would surface untranslated), each with a severity, ordered most-severe-first so a truncated list shows what matters. Two judgment calls, both mutation-tested: a **revoked** connection is `info`, not an error — the user chose it, and it is reported only so a missing chart has an explanation; and a revoked or reauth-needing connection is **not also called stale**, because staleness is the consequence rather than the actionable finding. Carries **no health values** — codes, severities, and provider names only. **Derived on read, not stored**: the design lists a `data_quality_findings` table but specifies no detector or resolution lifecycle, and a persisted finding needs one or it outlives the condition and tells the user to fix something already fixed. Trends is multi-metric exploration in one request instead of a client fanning out N calls, built on the **same** service as the single-metric read so the two can never disagree about a value or its baseline — a second query path would eventually drift, and a test asserts the two responses are byte-identical for the same window. Order is preserved so a client can pair response with request by position (mutation-tested). An unknown metric is a **404 naming it**, not a silently shorter list that would read as "no data for that metric" (mutation-tested). **No cursor**, despite the spec's "cursor/window params": the payload is bounded by the metric registry (eight) times the capped window — 1440 points worst case — so the honest limit is how many metrics one request may ask for; a cursor over a fixed set of eight would be ceremony without benefit. It lives at the documented `/v1/trends`, not under `/v1/metrics`, where `trends` would also shadow a metric name. The metric series was previously called blocked on "a windowed series surface that doesn't exist" — wrong: the nine `daily_*` feature queries the recovery components already read *are* that surface, so a chart and a score cannot disagree about what was measured. Eight metrics ship (hrv, resting_hr, temperature_deviation, respiratory_rate, sleep_duration, sleep_efficiency, steps, strain_load), each with its unit and the **baseline context** the design requires charts to carry (centre + robust scale, so a band can be drawn). **Measurements, not scores** — v0.1.0 ships one score code, so a per-metric rating would imply a formula nobody accepted, and a test asserts no score-shaped field appears. Unknown days are **omitted, never zero-filled**, with `known_days`/`coverage_is_partial` disclosed so a chart shows a gap rather than a measured zero (mutation-tested — zero-filling also silently corrupts the baseline, failing 3 tests). An unexposed metric name is a 404 rather than an empty series that reads as "no data" (mutation-tested), and `GET /v1/metrics` lists the names so a client need not discover them by 404. The latter two satisfy ADR-0005's *inspectable* requirement and the product principle that a user can audit **why** a day looks the way it does: the precedence was a private constant and the recorded decisions had no reader, so neither was answerable. `/effective` returns the ordered precedence with its policy version, listing **only families the engine actually selects between** — the ADR names authoritative providers for vitals/activity/workouts too, but no code chooses between providers there (one connector supplies each), and listing them would present an aspiration as an enforced rule. `/decisions` returns what was chosen for a local day and which providers competed, resolved to **provider names, never fact ids** — the provenance surface already refuses to hand out row ids and this must not be the back door (mutation-tested: leaking an id fails two tests). Tenant-scoped (mutation-tested); a day with nothing recorded is a 404 rather than an invented default. `GET/PUT /v1/source-policies/override` is deliberately **not** built: the policy is code-defined in v0.1.0, so a settable endpoint over a constant would imply a control that does not exist. Disconnect implements the settled roadmap decision (2026-07-19): it **revokes credentials and preserves history** — the stored secret is deleted and the connection marked `revoked` in **one transaction** (a crash between them would leave a connection the user believes is disconnected still holding usable tokens), while facts are untouched; only an explicit privacy delete removes those. The revoked connection stays listed so the user can see what happened, and a sync against it is a 409 rather than a doomed job. Unknown and cross-tenant are an indistinguishable 404 that revokes nothing. Audited as `connection.revoke`, including refused attempts. Both properties mutation-tested. **Vendor-side token revocation is deliberately not claimed** — no connector implements a revoke endpoint, so the local secret deletion is what actually stops this system using the grant. The confirmation issuer is what makes an `ALWAYS` tool usable at all: the design requires the user to approve **out-of-band via API, not the model**, and without an issuing endpoint `privacy.delete` would have been registered but permanently 403 — worse than not shipping it. It is authenticated and CSRF-enforced, binds tenant/user from the session, mints a 5-minute one-time token stored hash-only, and **refuses to confirm a tool that needs no confirmation** (409) so tokens nothing checks cannot turn the step into a rubber stamp. **The debug stand-in is gone**: the connections list answers *is my data actually flowing?* — linked providers, per-connection status, last successful sync, failure streak, and ingest counts — authenticated and tenant-scoped from the session. It carries **no health values**, and the error field is an error *class* only, so a failing connector cannot leak a vendor body into a user-facing surface. Still `partial` — a per-connection detail/unlink surface is not built |
| Tool registry (typed facade, AI-independent) | yes | yes | `ToolRegistry` + `Tool` with declared metadata (scopes, sensitivity, `side_effect`, `model_exposure`, `requires_confirmation`); Pydantic input/output validation; tenant from the tool **context**, never the input. Read-health tools `health.get_today`/`get_recovery`/`get_sleep` wrap the surface services with no formula. Exposed over `/v1/tools` (list + invoke) — the phase-two "**tools usable by REST without model packages**" exit criterion, proven end to end (CSRF-enforced invoke, 404/422 handling). Imports no model/AI SDK. **The catalog now covers every shipping read surface**: alongside `health.get_today`/`get_recovery`/`get_sleep`, the registry exposes `health.find_anomalies`, `health.get_recent_workouts`, `health.get_workout`, and `connections.list` — the canonical-registry names whose backing surfaces exist. `connections.list` is scoped `read:connections` with `sensitivity=low`, **not** `read:health`: connection metadata is statuses, timestamps, and counts, and folding it into the health scope would over-grant a caller that only needs a day view. Non-disclosure carries through to tools — `find_anomalies` omits the detector's internal `z_like` exactly as `/v1/anomalies` does, so a model never relays a bare z against a private baseline. A tool whose subject does not exist raises `LookupError`, which the invoke route maps to a generic **404** (`not_found`); unknown and cross-tenant are indistinguishable, so a tool cannot become an id-probing oracle (mutation-tested — without the mapping it is a 500). **Confirmation enforcement now ships.** `requires_confirmation` was previously *declarative only* — reported in the tool list and enforced nowhere, so a tool declaring it would have executed unconfirmed. The invoke route now **fails closed**: a tool requiring confirmation runs only when a confirmation redeems against the full binding the design specifies (`tenant_id` + `user_id` + `run_id` + `tool_name` + canonical args hash + idempotency key). Pure `domain/confirmations.py` owns the binding check and the canonical args hash (sorted-key compact JSON, so the user approves *values* not a serialization; unserializable values raise `TypeError` and NaN raises `ValueError` rather than being coerced into a binding that could never re-match). `tool_confirmations` (migration `0025`) stores **only the SHA-256** of the issued token, like sessions and provenance handles, with a partial unique index keeping at most one *pending* confirmation per `(tenant, idempotency_key)`. Consumption is a conditional CAS on `status = 'pending'` inside the same transaction as the binding check, so a lost update cannot authorize twice. All four design rules are tested end to end: the user confirms out-of-band, **argument substitution is refused** (a model swapping `a` for `b` after approval does not execute), a replay executes the side effect exactly once, and every rejection is the **same generic 403** so a caller cannot probe for valid tool names or live tokens (mutation-tested — disabling enforcement fails five tests). **`connections.sync` now ships as the registry's first mutating tool** (`side_effect=enqueue_job`, scoped `write:connections`), enforcing the canonical registry's **"yes if agent"**: confirmation is required for calls carrying a `run_id` and not for a direct session call, which is already an explicit CSRF-enforced human act. Both directions are mutation-tested (over-requiring fails 4 tests, under-requiring fails 5). It enqueues the *same* `connection.incremental_sync` job webhooks and the reconcile sweep use, so an agent cannot reach a sync path a user could not. **`privacy.delete` now ships too**, and forced the confirmation rule to become per-tool. The canonical registry states *three* answers — `no`, `yes if agent`, `yes always` — which a single bool could not express: the old global gate (`requires_confirmation and run_id is not None`) would have let a **direct** call erase a tenant unconfirmed. `ConfirmationPolicy` (`NEVER`/`IF_AGENT`/`ALWAYS`) now lives on the tool and the route asks it, so `privacy.delete` is refused without a confirmation from **every** caller (mutation-tested — dropping the `ALWAYS` branch fails the direct-call test). It is `sensitivity=destructive`, `model_exposure=False` (a model must never be what invokes irreversible erasure), and `side_effect=destroy_data` — a **new** vocabulary value, because the registry's `enqueue_job` assumes an async pipeline and ours completes inline; "queued" would imply a cancellable window that does not exist. `exports.create` remains unregistered (no service wiring). Note `model_exposure` is **declared but not yet enforced** — there is no agent caller to enforce it against; `confirmation` is what actually guards a mutation today. `run_id` is nullable and part of the binding, so a confirmation issued outside a run never authorizes one inside it |
| `POST /v1/checkin` subjective check-in (first write path) | yes | yes | cookie-authenticated **and** CSRF-enforced (state-changing method), tenant from session; validates the three normalized inputs to [0, 1]; versioned write (re-submission supersedes). Verified end to end that a recorded check-in feeds the subjective factor into `/v1/recovery` |
| `GET /v1/sleep` deterministic sleep summary | yes | yes | authenticated, tenant from session; exact `sleep_summary_v0.1.0` adherence + 14-day debt; a **summary not a score** — no score field is exposed; unknown days disclosed as a lower bound; golden + end-to-end tests |
| `GET /v1/recovery` recovery surface | yes | yes | authenticated, tenant from session; runs the full assembled scoring path (`general_recovery_v0.1.0`) and returns score/status/confidence/coverage, present factors, and `data_gaps`. Recovery is the **only** shipping score code. A tenant **with** overnight HRV/RHR and a mature baseline gets a real 0-100 score with HRV/RHR factors; a sleep-only tenant is honestly `insufficient` disclosing `missing_hrv_or_resting_hr`. A fabricated-score regression is guarded at the HTTP boundary. End-to-end tested both ways |
| Score persistence (`daily_health_scores`, `score_factors`) | yes | yes | migration `0014`; versioned like facts (partial unique index on current `(tenant, day, score_code)`; supersede, never rewrite). `ScoreRepository.write_recovery_score` is idempotent by `dependency_hash` (identical recompute is a no-op) and persists signed factor rows (sign from the component's side of the 50 midpoint). Registry + `null-score-iff-insufficient` CHECKs enforced at the DB. `derivation_run_id` links a written score to its derivation run (migration `0020`) |
| `score.recompute` job handler | yes | yes | assembles the recovery surface for a local health day, persists it through the versioned score repository, and **detects + tracks anomalies** for the day; runs through the **real worker runtime**. A full-history day writes a real score with signed factors; a sleep-only day writes an honest `insufficient` row (a stored outcome, not an absence); redundant recompute is idempotent; a malformed payload dead-letters |
| Automatic recompute chaining (`raw.normalize` → `score.recompute`) | yes | yes | after writing facts, normalize enqueues one recompute per affected local health day, keyed `recompute:<revision_id>:<day>` so a normalize retry does not stack duplicates while a new revision (a correction) triggers a fresh recompute. Proven end to end through the real worker: sync → normalize → recompute → persisted score row |
| Derivation lineage + opaque provenance (`derivation_runs`, `derivation_inputs`, `GET /v1/provenance/{token}`) | yes | yes | migration `0020`; the `score.recompute` handler records a `derivation_run` (formula version, status, confidence, freshness) for each written recovery score and links it via `daily_health_scores.derivation_run_id`. The run mints an opaque `provenance_token` — a **public but unguessable** handle (stored in the clear, unique-constrained, `uq_derivation_token`), not a secret — that `/v1/today` returns as `provenance_url`. `GET /v1/provenance/{token}` is authenticated and **tenant-scoped**: it resolves the token to disclosed lineage (kind, versions, status, freshness, input **roles**) and never leaks a table, raw, or run id; an unknown token and a cross-tenant token are the same 404 (a token cannot be probed). **Typed fact-id inputs now ship**: `score.recompute` records one `derivation_inputs` row per contributing fact, with a real `fact_record_id` FK, so a score resolves to the exact fact versions it read — the last hop of "traceable back to the raw payload". The role is the component code (matching the disclosed factor rows), mapped to its fact entity type by the pure `domain/derivation_roles.py`. Only **present** components contribute: a component omitted for an absent value or immature baseline read no fact, so recording one would assert an input the formula never used (verified — a sleep-only day records no `hrv`/`resting_hr` input). Sleep collapses to the **authoritative provider**, so a candidate that lost source selection never reads as an input. `subjective` is deliberately unmapped — a check-in is not a fact record and `derivation_inputs` has no typed FK for one, so it is absent rather than pointed at a wrong table; a completeness test fails if a new component is left silently unmapped. The **public surface is unchanged**: `GET /v1/provenance/{token}` still discloses roles and never ids, and resolution is now `DISTINCT` on role — a run stores one row per fact, so a role backed by several facts would otherwise repeat and leak the day's fact count, which the roles-only contract exists to avoid (mutation-tested). Fixed in passing: `create_run` inserted the run and its inputs in one flush with no ORM relationship between them, so SQLAlchemy could batch the children first and fail the `derivation_run_id` FK — latent because every prior caller passed an empty input list. Repository + route + recompute-traceability tested end to end |
| Serve stored scores on read (`/v1/recovery`, `/v1/today`) | yes | yes | `ServedRecoveryService` returns the persisted current score (with its factors, and gaps re-derived from present factor codes via the shared pure rule), falling back to compute-on-read only for a day never scored. Both surfaces read from storage; the compute path stays for the recompute handler. The served `version_n` and `freshness_at` are disclosed in the response body (null when computed on read); `/v1/today` carries the recovery score's `freshness_at` at top level. Tested via a fake reader (branching) and end to end (a seeded score served verbatim, computed-on-read has null metadata) |
| `GET /v1/today` composite surface | yes | yes | authenticated, tenant from session; stitches the recovery score, the sleep summary, the **deterministic training label**, and the **primary/supporting recommendations**, mirroring the recovery status at top level. Strain and activity **do not ship** in v0.1.0 — absent from the body and disclosed as `*_not_available` gaps, never fabricated; a no-sleep day omits the sleep block (no phantom zero-duration measurement) and gaps are deduplicated. The training label now applies the persisted-anomaly floor, the day's **descriptive ACWR** over-load downshift (from workout load), and the check-in **symptom-burden** floor. Carries the served score's opaque `provenance_url` (null when computed on read, no persisted run). End-to-end tested |
| Worker entry `python -m akunaki.worker` runtime | yes | yes | claim loop + SIGINT/SIGTERM cooperative shutdown; JSON logs |
| Worker runtime (claim → execute → heartbeat → settle) | yes | yes | port-typed in `application`; fake-repository policy tests + file-backed end-to-end tests |
| Retry classification + exponential backoff policy | yes | yes | transient/permanent/cancelled; capped jitter; min 1s (second-precision lifecycle) |
| Handler registry (`system.noop` built in) | yes | yes | unregistered `job_type` dead-letters instead of burning attempts |
| Background lease heartbeat | yes | yes | daemon thread; lease loss suppresses completion (no false success) |
| Leader-gated reaper tick (requeue expired / dead-letter exhausted) | yes | yes | standby never reaps without the `core-reaper` leader lease |
| Leader-gated periodic scheduler | yes | yes | pure `due_schedules` decides which `ScheduleSpec`s are due (never-fired → due immediately; else interval-elapsed; mutation-tested — the interval boundary and the never-fired branch); the worker fires them **only under the `core-reaper` leader lease**, on the reaper tick, tracking last-fired in memory, so at most one worker schedules. Each fire is **idempotency-keyed** so a lost lease or crash mid-fire never duplicates — and because that key is released once the job settles (see the enqueue row), the schedule keeps firing every interval instead of stopping after its first run. `stats.scheduled` counts **created** enqueues only, with deduped fires counted separately as `schedule_deduped`, so a schedule that is silently deduped every tick can no longer report healthy. **Now wired in `python -m akunaki.worker`**: the reconcile-sweep schedule fires every 30 min under the system tenant, and the full product registry gives the syncs it fans out a handler — proven end to end that a sweep job enqueues an incremental sync that has a handler (no dead-letter) |
| Production worker handler registry (`build_registry`) | yes | yes | `adapters/wiring/registry.py` assembles every product handler from concrete adapters (per-provider fetch clients, the sealer, DB repos) and the worker entrypoint uses it. A **`ProviderDispatchSyncHandler`** routes an initial/incremental sync to the per-provider handler for its connection's provider (one worker syncs all providers; a provider without a handler is a permanent error). Registry-completeness + full sweep→sync chain tested against a real DB + worker; the entrypoint boots the registry (needs a KEK, as a real sync opens sealed tokens) |
| Core-only / no model SDKs | yes | yes | import-linter + tests |
| Ruff / mypy / import-linter / pytest / pip-audit gates | yes | yes | see evidence docs |
| CI workflow (GitHub Actions) | yes | yes | four jobs: quality, migrations up/down/up on an ephemeral DB, core-only boot with models disabled, advisory `pip-audit`; every step verified locally before commit |
| Core-only boot proven without dev deps | yes | yes | CI installs with `--no-dev` and asserts no model SDK is importable, then boots API + worker with no `MODEL_*` config |
| Frontend / web | no | no | deferred |
| Session layer (`users`, `sessions`: issue, validate, rotate, revoke, purge) | yes | yes | migration `0011`; **hash-only** storage of cookie token and CSRF secret; expiry + revocation enforced; rotation revokes the predecessor. **The purge now actually runs**: `purge_expired` existed on sessions, login states, and OAuth states — each with an `expires_at` index built for a sweep — but had **no production caller**, so every expired row kept its stored secrets (hashed session tokens and CSRF secrets, sealed PKCE verifiers) indefinitely. A leader-gated `retention.sweep_expired` schedule (hourly, system tenant) now sweeps all four short-lived stores; `tool_confirmations` had no purge method at all and gained one. Deletion is on **expiry alone**, so a live row can never be swept out from under a user (mutation-tested — dropping the expiry filter fails 2 tests). A **consumed** confirmation is kept until it expires: its job is to make a replay fail, and past expiry it fails on expiry alone. One broken store does not abort the sweep — the others still run and the job raises at the end so retry policy sees it (mutation-tested) |
| OIDC login state (`login_states`: hashed `state` + `nonce`, sealed PKCE verifier) | yes | yes | migration `0012`; separate from `oauth_states` because login has **no tenant yet** and needs a nonce; single-use, expiring, exact redirect match |
| `id_token` claim validation (iss, aud, nonce, exp/nbf/iat, sub) | yes | yes | pure and clock-injected; 60s skew tolerance; rejected tokens never yield an identity; email redacted in `repr` |
| OIDC client (discovery, authorize URL, token exchange, JWKS signature verification) | yes | yes | PyJWT against the issuer's JWKS; **asymmetric algs only** (HS256 refused — alg-confusion closed); real-HTTP verified; secrets never logged |
| OIDC login flow (`/auth/login`, `/auth/callback`, user upsert, session issue) | yes | yes | port-typed service; state sealed before redirect, single-use consume, verify-before-session; first login provisions tenant+user; end-to-end verified against a real HTTP IdP through to an authenticated `/v1` request |
| User provisioning (one user per tenant, MVP) | yes | yes | keyed by `(oidc_issuer, oidc_subject)`; first login provisions tenant + user atomically; email refreshed but never used as identity |
| Session cookie wiring + CSRF enforcement | yes | yes | `Secure`/`HttpOnly`/`SameSite=Lax`; CSRF required on POST/PUT/PATCH/DELETE; one generic 401 so unknown/expired/revoked are indistinguishable |
| `GET /v1/session`, `POST /v1/session/logout`, `POST /v1/session/logout-everywhere` | yes | yes | tenant comes from the validated session, never a request parameter; logout revokes server-side **and** clears the cookie; logout-everywhere revokes every live session for the user (stolen-device / password-change) leaving other users untouched, CSRF-enforced |
| Connectors (Oura, Google Health, Polar) | partial | partial | Oura OAuth, fetch, initial sync, and sleep normalization ship. **Polar** fetch client, workout normalizer, and now the **provider-parameterized backfill** (`sync_config_for_provider`) ship — the same initial-sync handler backfills a linked Polar connection into `workout_sessions`, proven end to end; Polar **OAuth token exchange** (`PolarOAuthClient`) ships. **Google Health** now has its v4 fetch client, sleep-segment normalizer, backfill config, and **OAuth client** (`GoogleHealthOAuthClient`: PKCE + `access_type=offline`/`prompt=consent` for a refresh token, form-body client_secret) — the same handler backfills a Google Health connection into `sleep_sessions`, proven end to end. The **linking service now handles all three providers** (PKCE and non-PKCE via `uses_pkce`), and the **authenticated HTTP link routes** (`/v1/connections/{provider}/authorize`+`/callback`) drive a full browser link end to end. **Incremental sync** ships too — `connection.incremental_sync` resumes from the stored cursor (minus overlap) instead of a full lookback, degrading to a lookback on the first run. A resumed window is widened backwards to a provider's **vendor-imposed minimum range** (`SyncConfig.min_window`; Google Health v4 rejects any range narrower than **14 days**), because a cursor minutes old would otherwise produce a request the vendor refuses outright; the re-read days dedupe on content hash, so widening costs vendor calls but never duplicate revisions. **Oura, verified 2026-07-25 against the official OpenAPI spec** (`api.ouraring.com/v2/static/json/openapi-1.37.json`, found via the `redoc spec-url` in the docs page source): the five stream paths the fetch client uses (`sleep`, `daily_sleep`, `daily_readiness`, `daily_activity`, `workout`), the `start_date`/`end_date`/`next_token` params, and every field both normalizers read — `bedtime_start`/`bedtime_end`, `total_sleep_duration`, `time_in_bed`, `light`/`deep`/`rem_sleep_duration`, `awake_time`, `efficiency`, `average_hrv`, `lowest_heart_rate`, `average_breath` — all match, and the duration fields are confirmed **in seconds**. `temperature_deviation` is confirmed **absent** from the sleep model and present on nested `readiness`, which is exactly the fallback the vitals normalizer already reads. **Scope:** every endpoint in the spec declares `OAuth2: []` — an *empty* scope list — so Oura publishes **no per-endpoint scope mapping at all**. Of the eight real scopes (`email`, `personal`, `daily`, `heartrate`, `workout`, `tag`, `session`, `spo2Daily` — note `spo2Daily`, not `spo2`), `daily` is the only one whose description mentions sleep, so it stays the ask. The operational risk is that an **under-scoped Oura token returns an empty array, not an error**, making a scope shortfall look identical to an empty account; `InitialSyncHandler` therefore **warns when a full-lookback backfill yields zero records** (still a success — a new user genuinely has no data — but no longer silent). **Webhooks** ship for **all three providers**: Oura/Polar verify an HMAC-SHA256 body signature, and Google Health verifies its Google-signed push OIDC token (rotating JWKS keys + audience/service-account claims). A verified delivery is recorded once in a deduplicated inbox and triggers an incremental-sync refetch. The full connector lifecycle (link → backfill → webhook/incremental sync → normalize → source-select → score) is now complete for all three. **Manual sync ships too** — `POST /v1/connections/{id}/sync` (authenticated, CSRF-enforced, `Idempotency-Key` required) closes the phase-one "manual sync + connection health" scope item. It enqueues the **same** `connection.incremental_sync` job the webhook and reconcile paths use, so "sync now" has no separate semantics: it resumes from the stored cursor and dedupes on content hash like any other. The caller's key is namespaced per connection, so a double-clicked button queues one job (`created: false` on the repeat) rather than a queue of duplicates. A `needs_reauth` or `revoked` connection is a **409**, not a queued job doomed to burn attempts — the same rule the reconcile sweep applies when it skips those; unknown and cross-tenant are an indistinguishable 404 that queues nothing. Both guards mutation-tested |
| Agent / model packages | no | no | forbidden in core |
| Full data-model schema | no | no | tenants, durable-job lifecycle, connection lifecycle, OAuth state, sync transport, and the **sleep** fact slice exist; a **deterministic sleep source-precedence** ships and is now **persisted** as versioned `source_selections` + candidates (daily-metric slice, migration `0023`); the session/workout `source_grains`/`source_grain_versions`/`source_grain_members` grain tables remain pending (deferred until cross-provider episode matching), along with the `source_policies` rules tables (policy is code-defined for now) |
| Envelope encryption (AES-256-GCM, KEK/DEK, rotation, AAD binding) | yes | yes | fresh DEK+nonces per seal; versioned KEK registry; fail-fast boot without keys; mutation-checked randomness |
| Sealed tokens persisted to `connection_secrets` | yes | yes | raw column holds no readable token; envelope bound to its connection; cascade delete |
| KEK sourcing from external KMS / secret manager | no | no | keys load from `AKUNAKI_SECRET_KEKS` only; no KMS client, rotation runbook, or key-use audit |
| OAuth state / PKCE handshake primitives (`oauth_states`) | yes | yes | migration `0005`; hashed state only, sealed PKCE verifier, exact redirect match, single-use + expiry, purge sweep |
| Oura OAuth client (authorize URL, PKCE code exchange, refresh) | yes | yes | S256 only; typed failure vocabulary (`invalid_grant` → reauth vs retryable); secrets never logged; mock-transport + real-HTTP verified |
| OAuth linking service (start link → callback → sealed tokens) | yes | yes | port-typed in `application`; `tenant_id` is a parameter; connection row + sealed secret written in **one** transaction |
| Connection repository (link/relink, status transitions) | yes | yes | relink reuses the existing `(tenant_id, provider)` row; failed exchange leaves no half-written connection |
| Sync transport schema (`sync_runs`, `raw_payload`, `sync_cursors`, `raw_objects`, `raw_revisions`) | yes | yes | migration `0006`; every response retained (hash indexed, **not** unique); append-only revisions; `superseded` rejected as a tombstone reason |
| `webhook_inbox` table + webhook verification | yes | yes | migration `0021`; `webhook_inbox` durable deduplicated deliveries, unique on `(connection_id, dedupe_key)`, with the sole one-way FK `body_payload_id` → `raw_payload` (nullable, `SET NULL`) and a `json_valid` header-meta CHECK. Pure `webhook_verification` does **constant-time HMAC-SHA256** over the exact body (Oura/Polar). **Google Health** verifies its Google-signed push OIDC token: `GooglePushVerifier` checks the Bearer JWT's signature against Google's rotating **JWKS** (PyJWT, asymmetric algorithms only — HS256 downgrade rejected), then the pure `validate_google_push_claims` checks issuer, audience, expiry (with a 60s skew, boundary + skew mutation-tested), and the push **service-account** email (`email_verified`). Both verifiers are **mutation-tested** (a known-answer SHA-256 digest pins the HMAC algorithm; the empty-secret guard is proven to fire before the compare). `WebhookInboxRepository.record_delivery` is an atomic `INSERT ... ON CONFLICT DO NOTHING` returning duplicate-or-new. `POST /webhooks/{provider}/{connection_id}` (unauthenticated — vendor-called, trust from the signature/token) verifies, records, acknowledges fast, and enqueues a `connection.incremental_sync` refetch (idempotency-keyed). A bad signature/token, tampered body, wrong service account, or unknown connection is an indistinguishable 401; an unconfigured provider a 404; a redelivery is deduped (no second job). The webhook only **triggers** a pull — it never trusts the delivered body as data. Verified end to end over HTTP for all three providers |
| Oura V2 fetch client (windowed pages, pagination, typed failures) | yes | yes | exact body returned for faithful transport persistence; 401/403 → `unauthorized`, 429 → `rate_limit` with `Retry-After`; token never logged |
| Atomic fetch commit (`IngestionRepository`) | yes | yes | transport row + logical revision + cursor in **one** transaction; revision appended only on new `content_hash` |
| `connection.initial_sync` handler | yes | yes | opens sealed tokens, paginates, translates fetch outcomes into the worker's retry vocabulary; auth failure → `needs_reauth` + dead letter, 429/5xx → retry |
| `connection.incremental_sync` handler | yes | yes | reuses the initial-sync page loop (extracted to a shared `sync_window`) but sets the window start from the **stored cursor minus overlap** rather than a full lookback, so only new data is pulled; the overlap re-fetches recently-finalized records (content-hash dedupe makes an unchanged re-fetch a no-op). First run (no cursor) degrades to the lookback backfill; a malformed cursor falls back to the lookback rather than wedging the connection; a cursor older than the lookback horizon is clamped to it. Verified against a real DB + worker: first-run window, cursor resume, malformed-cursor fallback, stale-cursor clamp |
| `connection.reconcile_sweep` handler | yes | yes | scheduled reconciliation that catches gaps a missed webhook or sync left behind: finds **active** connections whose last successful sync (`connection_health.last_success_at`, now written on every ACTIVE transition) is older than a staleness cutoff (default 6h) or never synced, and enqueues an incremental sync for each — **idempotency-keyed per connection** (`reconcile:<id>`) so a connection already queued by a webhook or an earlier sweep is not double-scheduled — the key is held only while that job is in flight, so the connection becomes reconcilable again once it settles. `needs_reauth`/`error` connections are skipped (they cannot sync until re-consent). Real-DB stale query + fake-port handler tested |
| Vendor record identity in the **raw** layer | yes | yes | per-record: one page yields one `raw_object`/`raw_revision` **per record**, keyed `stream:<vendor_id>` (or a body hash when the vendor supplies none); each revision stores its own slice body (`slice_json`) |
| Stable ids for streams without a vendor id | partial | yes | falls back to hashing the record body — per-record, but a cosmetic vendor change re-identifies the record; only `sleep`/`daily_*`/`workout` have mapped id fields |
| Sleep fact schema (`fact_records`, `sleep_sessions`) | yes | yes | migration `0007`; versioned headers with a partial unique index on current; typed one-to-one detail (not EAV) |
| Oura sleep normalizer (pure, deterministic) | yes | yes | wake-date assignment, canonical minutes, honest quality grading; no clock, so re-runs are byte-identical. Honors the real Oura `type` enum: `long_sleep` is the **principal** session, `sleep`/`late_nap`/`rest` are naps, `deleted` is a tombstone (dropped, never ingested); a missing type falls back to the duration threshold |
| Versioned fact writes (supersede, never update in place) | yes | yes | identical content is a no-op; changed content appends a version and retains the prior row with its detail |
| Rolling baseline + robust z-score (`general_recovery_v0.1.0`) | partial | partial | pure domain: 42-day prior window, median center, MAD→robust_scale with the IQR fallback and per-metric floors, maturity gate (insufficient/min/mature at 14/28), z clamped to [-3, 3]; **no imputation** — missing days are absent, not zero; golden + mutation tested. **Takes the present sample series**; windowing from stored facts and the z→component wiring now ship (see the windowed-feature-queries row). Still `partial` because only the recovery formula consumes it — no other score version is accepted yet |
| Recovery composite engine (`general_recovery_v0.1.0`) | partial | partial | pure domain: exact component weights, baseline z-score→c curve, bounded adherence, piecewise freshness, the three-part sufficiency gate, weighted mean renormalized over **present** weights with disclosed coverage, and `confidence = coverage*freshness*quality*baseline_maturity`; golden + mutation tested. Takes already-mapped component scores; the baseline/ACWR/check-in mapping, persistence, and the `/v1` surface are all wired now (ACWR also feeds the training-label downshift on `/v1/today`) |
| Feature→component bridge | partial | partial | pure seam composing baseline z-score → directed `c` for baseline-dependent components and direct pass-through for sleep adherence; an insufficient baseline **omits** the component (returns None), never a midpoint. Verified end to end that **sleep-only data is honestly `insufficient`** (gate 2 needs HRV or RHR, which no fact table sources yet). Windowing from stored facts and the per-component feature queries now ship (see the windowed-feature-queries row). Still `partial` because the seam feeds the recovery composite only |
| Windowed feature queries + recovery-input assembly | yes | yes | `daily_sleep_efficiency`, `daily_hrv`, `daily_resting_hr`, `daily_temperature_deviation`, `daily_respiratory_rate`, and `daily_principal_sleep_midpoint` windowed queries (each omits days where the value is unknown), the 42-day `baseline_window_days` helper (target day excluded), and `RecoveryInputService` that builds every present component (adherence + consistency direct; efficiency/HRV/RHR/temperature/respiratory each present only with a mature baseline). Verified against a real DB that a tenant **with** overnight HRV/RHR and a mature baseline reaches a real score, while a sleep-only tenant stays `insufficient`. Prior-load/subjective still await their sources |
| Sleep consistency (mean resultant length) | yes | yes | pure `sleep_consistency`: principal-sleep local-time midpoints on the `[0, 1440)` circle over the 14-day window, `R = sqrt(mean(cos)² + mean(sin)²)`, score `100·R`; **direct 0-100, no baseline**; requires ≥7 valid nights or the component is omitted. Golden + mutation tested; verified end to end that regular sleep scores 100, <7 nights omits, and naps are not valid nights |
| Prior-load balance (descriptive ACWR) | yes | yes | pure `prior_load`: ACWR = acute (7-day) / chronic-weekly (28-day / 4) under **strict** coverage (any unknown day → undefined → component omitted); the exact band mapping (under-load toward 0, 0.8-1.3 balance band = 100, over-load to 0 at a≥2.0); the `all_zero_rest` defined-balanced case and zero-chronic/nonzero-acute undefined case. Golden + mutation tested. **Now sourced from workout facts** — `daily_strain_load` sums included session loads; verified against a real DB that full 28-day workout coverage activates the component (ACWR defined) and a coverage gap omits it |
| Canonical zone-load + Polar workout normalizer | yes | yes | pure `zone_load` (`session_load = Σ minutes_z · weight_z`, default Z1..Z5 = 1..5; daily strain-load = sum of included sessions) — canonical load computed **internally**, never a vendor field. Pure `polar_workout_v0.1.0` normalizer parses HR-zone durations (ISO-8601 or seconds) into `workout_sessions` (migration `0019`), local-start-date-keyed, incomplete-zone records skipped. Flows through the real ingestion loop via schema-version dispatch (`polar.*` → workouts). Golden + mutation tested. **The sessions are now readable**: `GET /v1/workouts` (cursor-paginated list, newest first) and `GET /v1/workouts/{id}` ship, authenticated and tenant-scoped. Until now the only read path over workout facts was the daily load **aggregate** feeding ACWR — one number per day — so the sessions themselves, with their zone minutes, were invisible. The surface discloses what was measured (start/end, per-zone minutes) plus the internally computed `session_load`; there is deliberately **no workout score**, since v0.1.0 ships exactly one score code and inventing a second would imply a formula that has not been accepted. A row flagged `exclude_from_load = 1` is **hidden from the list too**, not just from load math: that flag marks a *duplicate* of the same real session from a second provider, so showing it would present one workout twice. Pagination is **keyset** on `(start_utc, id)`, not offset — workouts arrive continuously and an offset would skip or repeat rows when a sync lands between pages — with an opaque base64 cursor and a `limit + 1` lookahead so an exactly-full final page does not advertise a page that isn't there. Both invariants are mutation-tested, including two sessions starting in the same second (without the id tie-breaker, paging silently drops every other row sharing a timestamp). Unknown and cross-tenant ids are the same 404. `/v1/workouts/{id}/swim` remains **unbuilt**: no swim lengths/distances are stored, so it would be fiction |
| Polar fetch client (AccessLink) | yes | yes | `PolarFetchClient` fetches the `exercises` list with Bearer auth; returns the **exact** body for faithful retention (token never logged or in `request_meta`), classifies 401/429/5xx/malformed to typed failures. Mock-transport tested (no network). **Not yet wired to a Polar-provider sync config/handler** — the fetch client + normalizer both exist; a `SyncConfig(schema_version="polar.v1", stream="workout")` path and Polar OAuth token exchange would complete a live pull |
| Subjective modifier (completed check-in) | yes | yes | pure `subjective`: `c = clamp(100·(0.5·energy_n + 0.25·(1−stress_n) + 0.25·(1−symptom_burden_n)), 0, 100)`; present **only** when all three normalized fields of a completed check-in are known (a missing check-in or blank field omits it, never a neutral 50). Golden + mutation tested. Sourced from `subjective_check_ins` (migration `0017`, versioned) via `POST /v1/checkin`. **This completes all 9 recovery component families** |
| Anomaly detectors + persistence (Stage 4) | yes | yes | pure `anomalies`: the six enabled v0.1.0 detectors (exact z-thresholds, moderate/high at the clamp), direction-aware clear checks, and the **2-consecutive-day clear state machine** (open/update/close with peak-severity retention). Persisted via the `anomalies` interval table (migration `0018`, one active per feature) through `AnomalyRepository` + `AnomalyTracker`. **The `score.recompute` handler now detects and tracks anomalies automatically**: `RecoveryInputService.feature_signals` computes each feature's z (the same z its component used) and the tracker advances the interval — verified end to end that a far-below-baseline HRV opens a `low_hrv` interval during recompute. A persisted high-severity anomaly floors the training label at `light` on `/v1/today`. Golden + mutation tested. **The low-activity anomaly is now live too** — daily steps ingest (see the activity ingestion row) feed `feature_signals`, and a far-below-baseline steps day opens a `low_activity` interval. All six v0.1.0 detectors are now sourced. **The intervals are now readable**: `GET /v1/anomalies` (authenticated, tenant from the session) discloses active *and* recently-cleared intervals with their feature code, severity, start/end days, and detector version. Until now the only surface reading anomalies was `/v1/today`, which collapses them to a single "is any active one high-severity" boolean that floors the training label — enough to change a recommendation, not enough to explain it. Cleared intervals stay listed for a bounded window (default 14 days, matching sleep debt; capped at 90) because an anomaly that opened Tuesday and cleared Thursday is what explains a past day; an interval that is **still open** is always listed regardless of the window. The detector's internal `z_like` is **deliberately never disclosed** — anomalies are non-diagnostic flags against a private baseline, and a bare z-score invites exactly the over-reading that framing exists to avoid; severity is the disclosed strength. The reference `day` is **required**, like every other day surface, so the server never guesses a tenant's local health day from its own clock. Tenant scoping is mutation-tested |
| Training label + recommendations (Stage 5) | yes | yes | pure `training_label` (`training_label_v0.1.0`: recovery-band base + ordered downshift; missing data never rests, high-severity anomaly floors at light) and `recommendations` (the exact rule predicates + priority/conflict-group resolution to one primary). Golden + mutation tested. Wired into `/v1/today`: `training_recommendation` + `primary_recommendation`/`supporting_recommendations` ship (verified end to end). **Both the anomaly and ACWR downshift/rule paths are now threaded**: a persisted high-severity anomaly floors the label at light, and the day's descriptive ACWR (from `RecoveryInputService.acwr_for_day`, the same load windows the prior-load component reads) drives the over-load downshift — proven at the composite that ACWR > 1.3 caps a HARD day at MODERATE while an undefined ratio leaves it untouched. The **symptom burden** is threaded too (from `RecoveryInputService.symptom_burden_for_day`, the same completed check-in the subjective component reads): a burden ≥ 0.75 floors a HARD day at LIGHT, an absent check-in leaves it untouched. The resolution's **suppressed** rules are now served: they were computed correctly and then discarded by the composite, so `GET /v1/recommendations` carries them with `suppressed_by` naming the winner (mutation-tested at composite and route). The `severe_symptom_flag` REST trigger stays dormant — the v0.1.0 check-in has no distinct severe-symptom marker |
| Overnight vitals (HRV, RHR, temperature, respiratory) detail table + normalizer | yes | yes | migrations `0013`/`0015`/`0016`; `overnight_vitals` one-to-one detail (at-least-one-of-four invariant); pure `oura_vitals_v0.1.0` normalizer extracts `average_hrv`/`lowest_heart_rate`/`temperature_deviation` (flat or nested `readiness`)/`average_breath` from the sleep payload, wake-date-keyed like sleep, naps and empty-signal records skipped, sanity-bounded; flows through the real ingestion loop with shared lineage. **Correction (2026-07-25):** the nap guard compared `type == "nap"`, a value that **does not exist** in Oura's real `PublicSleepType` vocabulary (`deleted`/`sleep`/`long_sleep`/`late_nap`/`rest`, confirmed in the official spec). Because the comparison ran only when a `type` was present, every typed record bypassed the duration fallback — so `late_nap`, `rest`, and short `sleep` bouts were ingested as **overnight vitals and fed the HRV/RHR rolling baselines**, skewing the very inputs the recovery gate depends on. The guard now mirrors the sleep normalizer (only `long_sleep` is a principal session; the duration rule applies only when the vendor declares no type), and a `deleted` tombstone is dropped here too rather than yielding phantom vitals. The old test asserted the fictional `type="nap"`, so it never caught this; it is replaced by parametrized coverage over the real vocabulary. **HRV/RHR is what lets the recovery gate pass**; temperature (0.10) and respiratory (0.05) add components |
| Daily activity (steps, active minutes) detail table + normalizer | yes | yes | migration `0022`; `daily_activity` one-to-one detail (steps INTEGER, active_minutes; at-least-one-signal + non-negative CHECKs). Pure `google_activity_v0.1.0` normalizer reads Google Health daily-activity data points, local-date-keyed, sanity-bounded (zero-steps kept as confirmed rest, the cap boundary mutation-tested), empty-signal days dropped. Flows through the ingestion loop via the `google_activity.` schema-version dispatch into `daily_activity`. `FactRepository.daily_activity_steps` feeds `feature_signals`, so a far-below-baseline steps day opens the `low_activity` anomaly (golden + real-DB tested). The activity **score** stays blocked (no accepted formula) |
| Sleep source selection (multi-provider precedence) | partial | yes | pure `source_policy_v0.1.0`: a deterministic sleep-provider precedence (`authoritative_sleep_provider`) — **Oura wins any day it covers, Google Health is the fallback**, no averaging and no cross-provider fallback. Applied in the three sleep feature queries (`daily_sleep_durations`/`daily_sleep_efficiency`/`daily_principal_sleep_midpoint`): when two providers cover a night the repo reads only the authoritative provider's sessions, fixing a **double-count** the second sleep connector would otherwise cause. Verified against a real DB (Oura 420 chosen over a 820 sum; efficiency never blended; a longer Google session is not the principal one) and **mutation-tested** (precedence order, present-check, and the no-authoritative-source None). The decision is **persisted and auditable, and now actually written**: `source_selections` (daily-metric slice, migration `0023`) records one **current** versioned decision per `(tenant, metric_family, day)` with the competing providers as `source_selection_candidates` (the "Why"); a changed decision supersedes (one-current partial unique index), `missing_authoritative` carries no fact + a required reason, and an identical decision is idempotent — real-DB tested. **The repository was previously unreachable from production code** — referenced nowhere outside its own unit tests — so the ADR-0005 audit trail existed as schema only and the "Why" was recomputed on every read and discarded. `raw.normalize` now records the decision for each sleep-bearing day it touches, wired in `build_registry`. The pure `decide_sleep_selection` ranks **every** provider covering the day: the winner, the lower-precedence losers (`eligible`, retained as inspectable alternatives — never a blend, never a silent fallback), and providers outside the sleep policy as `ineligible`, which keeps "no recognized source" distinct from "no data". `only_source` is distinguished from `policy_match` so a resolved conflict is visible as such. The decision is derived from **stored** facts rather than the payload in hand, so it accounts for every provider covering the night whichever revision arrives first; it is content-idempotent, so a normalize retry re-derives the same decision and writes no new version (mutation-tested end to end through the real handler). Still the daily-metric slice **only**: the session/workout `source_grains`/`source_grain_versions`/`source_grain_members` machinery is deferred until cross-provider episode matching is needed (one workout provider today) |
| Directed z-mapping modes | yes | yes | `Direction` enum with the four exact v0.1.0 forms: `+z` (HRV), `-z` (RHR), `-\|z\|` (temperature: any deviation worse), `-max(z,0)` (respiratory: elevated worse). Golden + mutation tested; all four now sourced |
| Other detail tables (labs, …) | no | no | **deliberately deferred**: each arrives with the normalizer that populates it, not as empty tables. Sleep, overnight vitals, workouts, and daily activity now ship |
| `raw.normalize` job handler | yes | yes | keyed by `raw_revision_id`; enqueued in the **same transaction** as the revision, so a revision can never exist without its normalize job |
| Closed ingestion loop (sync → normalize → facts) | yes | yes | drained through the real worker; verified end-to-end against a live HTTP server with lineage intact |
| Connector OAuth HTTP routes (authorize/callback) | yes | yes | `GET /v1/connections/{provider}/authorize` + `/callback` — **authenticated** (tenant from the session, never a parameter), wiring the provider-uniform linking service. Per-provider OAuth credentials come from config (`{provider}_client_id`/`_client_secret`/`_redirect_uri`); a provider not fully configured, or unknown, is an indistinguishable **404** (no half-built connect surface). `build_oauth_client` maps a provider to its concrete client; a transient exchange failure is 503, a permanent one 400. Verified end to end over HTTP: authorize → captured state → callback links an `active` connection (Polar, non-PKCE), unknown/unconfigured provider → 404, no session → 401 |
| Polar / Google Health fetch clients | partial | partial | **Polar** `PolarFetchClient` (AccessLink exercises) ships, is normalizer-wired, and has its **backfill config** (`sync_config_for_provider("polar")` → `workout`/`polar.v1`); the initial-sync handler backfills a linked Polar connection end to end (fetch → `polar.v1` revisions → `workout_sessions`). The fetch client reads the **non-transactional** `GET /v3/exercises?zones=true` — one call returning a bare JSON array of exercises with HR-zone durations already inlined. **Migrated 2026-07-26** off the `exercise-transactions` lifecycle (`POST` open → `GET` list → per-exercise summary + `heart-rate-zones` → `PUT` commit), which the official [swagger](https://www.polar.com/accesslink-api/swagger.yaml) tags **"Exercises (deprecated)"** and which directs partners to this resource. The deprecated path is **gone**, not kept alongside: one shape, four fewer HTTP calls per sync. Three real contract differences the migration had to handle — (1) the response is a **bare array**, not `{"exercises": [...]}`, so `split_page` now splits a top-level array (without it every exercise collapsed into one page-hash-keyed revision, destroying per-workout identity); (2) the schema is **snake_case** (`start_time`), not the transactional resource's hyphenated `start-time`; (3) — the trap — `start_time` is **naive local wall-clock time** whose offset arrives separately in `start_time_utc_offset`, so the two must be combined into an instant. A straight endpoint swap would have dropped **every** workout, since the RFC-3339 parser rejects naive datetimes; a record missing the offset is now skipped rather than pinned to a guessed timezone. Two behavioural consequences, both handled: there is **no commit step**, so the vendor re-delivers the same exercises every call (absorbed by content-hash dedupe, making a repeat sync a no-op), and only exercises uploaded to Flow in the **last 30 days** are returned at all. The **Polar OAuth client** (`PolarOAuthClient`: authorize URL + Basic-auth code exchange, no PKCE, no refresh token, captures `x_user_id` as the connection's `external_user_id`) now ships. The **linking service now links a non-PKCE provider** (Polar) end to end — a `uses_pkce` capability on the client port makes `start_link`/`complete_link` thread a verifier only for PKCE providers, sealing an empty placeholder otherwise (the state's CSRF single-use protection is identical); the connector link routes now ship (see the connector OAuth HTTP routes row), so a browser can link Polar end to end. **Google Health** now ships its **v4 fetch client** (`GoogleHealthFetchClient`: `dataPoints` **GET** with an AIP-160 `filter` over `sleep.interval.start_time`, `pageSize=25`, `nextPageToken` pagination — sleep stream) and a pure **sleep-segment normalizer** (`google_sleep_v0.1.0`: reads each data point's `sleep.stages[]` (`type` `LIGHT/DEEP/REM/AWAKE/ASLEEP`, session `interval` fallback), aggregates into one canonical session per wake-date, shared `SleepFact` output; mutation-tested — session-bound min/max, sleep-vs-awake split, and the zero-duration-segment guard). This corrects the earlier `dataPoints:reconcile` POST and the flat `{startTime, sleepType: SLEEP_STAGE_*}` read, neither of which matched the real v4 shape. **Corrections (verified 2026-07-25 against the official [v4 REST reference](https://developers.google.com/health/reference/rest/v4/users.dataTypes.dataPoints/list)):** three identifiers were wrong and are now fixed — the data type id was the legacy Google Fit `com.google.sleep` (v4 uses the bare `sleep`), the stage fields were `sleepStages[]`/`stageType` (v4 uses `stages[]`/`type`), and the OAuth scope was `.../auth/health.sleep.read` (the real scope is `.../auth/googlehealth.sleep.readonly`, and all Google Health scopes are **Restricted**, requiring Google's security review). `ASLEEP` (undifferentiated sleep) now counts toward duration without being attributed to a light/deep/rem stage it never described. **These were never caught end to end because the tests asserted the same wrong identifiers** — the "proven end to end" claim below covers fakes, not a live Google call. Regression tests now fail if any of the three legacy spellings return. Wired into the backfill (`sync_config_for_provider("google_health")` → `sleep`/`google_health.v4`) and normalize dispatch — a linked Google Health connection backfills into `sleep_sessions` end to end. The **Google Health OAuth client** (`GoogleHealthOAuthClient`: authorize URL with PKCE S256 + `access_type=offline`/`prompt=consent`, form-body client_secret code exchange, refresh) now ships too. With the linking service now provider-uniform and the link routes mounted, a browser can drive a full Google Health link |
| Concurrent worker runtimes (exactly-once execution, single leader, stolen-lease safety) | yes | yes | bounded local stress: 3 workers/24 jobs, 4 contending reapers, independent engines |
| Sustained multi-process fleet under production load | no | no | in-process threads with independent engines only; no long-running or cross-host soak |
| Product job handlers | yes | yes | `connection.initial_sync`, `connection.incremental_sync`, `connection.reconcile_sweep`, `raw.normalize`, and `score.recompute` ship, chained: sync enqueues normalize (same commit as the revision), normalize enqueues recompute per affected day; the reconcile sweep fills sync gaps a missed webhook would leave |
| Atomic domain side-effect unit of work | yes | yes | `FencedUnitOfWork` (`ports/unit_of_work.py` + `adapters/db/unit_of_work.py`) runs a job's side effect in **one** transaction that also carries the lease check, closing a real time-of-check/time-of-use hole: `has_valid_job_lease` answers "valid *now*", so a lease could expire between the check and the write and the superseded worker's write still landed. Its `complete_job` was correctly fence-rejected, but **the side effect was already durable** — for a versioned artifact that is corruption, not a duplicate: two workers computing the same day at different times get different results (so dependency-hash idempotency does not absorb it), the winner writes version *n*, then the stale worker supersedes it and appends *n+1*, making the **older** computation the current score with a supersession chain that records it as legitimate. The fence is now re-verified **inside** the write transaction, before and after `work` — the post-check is the load-bearing one (it spans the seconds `work` actually takes) and is **mutation-tested**: neutering it fails exactly the mid-flight test. `ScoreRecomputeHandler` persists through it (derivation run + score + factors + anomaly tracking are one unit, all-or-nothing), wired in `build_registry`. A lost lease raises `LeaseLostError`, which the worker runtime settles as **nothing at all** — not a failure — because recording one would burn the *new* owner's attempt budget under a dead fence. Proven end to end against a real DB that a stale worker writes **no** score row (and that the same test fails without the UoW), plus happy-path commit, expired-lease rollback, stale-worker-behind-new-owner, and handler-exception rollback |
| Remote production Turso (Turso Cloud) | no | no | **product deferred**; proposed in ADR 0003 only |
| Privacy delete (cancel jobs → scrub rows → completion proof) | yes | yes | phase-one exit criterion; ordering enforced (scrub before cancel is rejected); tenant-scoped; proof carries counts only. **Now reachable**: the pipeline was previously referenced nowhere outside its own unit tests, so ADR-0005's "only an explicit privacy delete removes facts" had no way to be invoked. The documented `POST /v1/privacy/delete` and `GET /v1/privacy/delete/{id}` now ship, driven by a `DeletionService` that runs the five stages in order and marks the request **failed** — retaining the stage reached — if any raises, so a half-run deletion never reads as completed. The delete is authenticated **and** CSRF-enforced, tenant from the session (a caller can only erase their own tenant), and **synchronous**: a 200 means the data is gone, not queued. It clears the session cookie, since every session cascades away with its tenant. The status read is **unauthenticated by necessity** — the tenant and all its sessions are gone by the time a completed request is worth reading — which is safe because the id is an unguessable UUID and the response discloses only a pipeline status; unknown and cross-tenant ids are the same 404. **Two fixes in passing:** the scrub named a model list written before vitals/workouts/activity/scores/anomalies/check-ins/derivations/selections existed — all were removed by tenant cascade, but they are now deleted **explicitly**, so erasure is a property of an auditable list rather than of FK declarations spread across a dozen migrations; and `ScrubCounts` counted only the original tables, so the completion proof — the audit artifact — **under-reported** what was erased (a tenant whose only data was a check-in got `rows_scrubbed: 0`). A schema-driven regression test now walks every `tenant_id`-bearing table after a deletion and fails if any row survives, so a future table cannot silently escape the scrub |
| Tamper-evident audit events (`audit_events`) | yes | yes | migration `0026`. The security model names audit as the control for **repudiation** ("I didn't delete that"), and every `Tool` already declared an `audit` name that **nothing ever wrote** — the same declarative-only gap `requires_confirmation` had. Now `privacy.delete` appends an event on both success *and* failure (a half-run erasure is the case most worth recording). Two properties are enforced, both mutation-tested: (1) **no health values** — `metadata_json` is a bounded key/value map and any key shaped like a measurement (`hrv`, `*_score`, `sleep_min`, `steps`, …) is **rejected**, not filtered, since silently dropping a key makes an incomplete record look complete; (2) **tamper-evident** — each event hashes its own content plus the previous event's hash, so an edited, removed, or reordered row breaks every link after it, and a `GENESIS_HASH` sentinel stops a truncated chain passing as a fresh one. Append reads the tail and inserts in **one** transaction, or two writers would fork the chain. `audit_events` has **no FK to `tenants`** by design: the record must outlive the tenant it describes, or a privacy deletion would erase its own proof (real-DB tested). Honest limit: a hash chain detects row-level tampering; it does **not** defend against an attacker who can rewrite the whole chain — that needs signed batches, as the security doc's "as implementation matures" allows. **`tool.invoke` and `connection.create` are now wired too.** `tool.invoke` fires for **mutations only** — a deliberate scope call, not an omission: the threat audit answers here is confused deputy (*actions taken*), and auditing the seven read tools would make the chain a write bottleneck on the hottest path, since every append serializes on a tail read and a dashboard polling `health.get_today` would add thousands of rows a day no reviewer will read. `Tool.is_audited` encodes that (mutation-tested — ignoring `side_effect` fails the reads-not-audited test). **Refusals are recorded, not just successes**: a rejected confirmation is exactly what a confused-deputy attempt looks like from outside, so auditing only successes would leave the attempts worth investigating as the ones with no trace (mutation-tested). An agent-originated call records `origin=agent_run`, the first thing a reviewer asks. `connection.create` fires on **linked**, **failed**, and cross-tenant **refused** — a failed exchange has no connection row, so it is attributed to the session's tenant. Neither path copies tool arguments or token material into the trail (tested); the confirmation binding's args hash is the non-health handle to a mutation's exact inputs. Both helpers never raise — the action already happened, so failing there would report an error for work that succeeded. **The chain is now actually verified.** It was tamper-*evident* with no detector — hashes nobody recomputed, which is theatre. A leader-gated `audit.verify_chain` schedule (hourly, system tenant) walks the chain and publishes `akunaki_audit_chain_intact` + `akunaki_audit_chain_verified_timestamp_seconds`, so an operator alerts on it instead of remembering to look; a **stale** timestamp is itself the alert that the verifier stopped running. It runs on the **worker, not a request path**: verification is O(chain), so an endpoint would hand any caller an unbounded scan. The verdict is **persisted** to `system_checks` (migration `0027`), because the gauge lives in the worker's own registry and the worker serves no scrape endpoint — a gauge alone reached nobody. `/readyz` (served by the API) reads that row, so the verdict is finally reachable; the table is a latest-known-state cell overwritten in place, not a history, so an hourly check never leaves rows for a probe to scan. Persisting never raises: the verification already happened and the gauge already carries it, so failing there would turn a successful check into a retry. Detected tampering **does not fail the job** — it is not transient, so a retry would burn the attempt budget and dead-letter, turning a standing alert into a one-off error nobody sees; the gauge stays at 0 until a pass. `verify()` also had to stop materializing the table (it only grows, so the deployment with the most history to protect would OOM first): it now walks in bounded batches via a new pure `verify_link`, with `verify_chain` expressed in terms of it so there is one rule rather than two that can drift. Both decisions mutation-tested (ignoring the verdict fails 2 tests; a `>=`/`>` paging off-by-one fails 3, including a tamper straddling a batch boundary). Remaining action in the vocabulary but unemitted: `export` (no export service) |
| Restoration-suppression ledger | no | no | **deliberately not built**: needs a dedicated deletion key with access separation; an empty table would imply a guarantee the system cannot make |
| Backup expiry on deletion | no | no | pipeline records the stage; no backup provider is wired, so nothing is actually expired |
| Multi-tenant fact isolation | yes | yes | fact identity is tenant-scoped (migration `0010`); regression-tested after a collision bug where one tenant's fact silently overwrote another's |
| Encryption-at-rest / backup evidence | partial | partial | application-level envelope for secret columns done (see [evidence](evidence/phase-zero-envelope-encryption.md)); platform at-rest, backup/export encryption, and key separation runbooks open |
| Volume / vector spikes | no | no | Phase Zero open |

---

## Process entrypoints

| Entrypoint | Behavior today |
|------------|----------------|
| `python -m akunaki.api` | Serves core API; `/healthz` only |
| `python -m akunaki.worker` | Boots config/DB, readiness, then runs the durable claim loop until SIGINT/SIGTERM |
| `python -m akunaki.agent_worker` | **not present** |
| `python -m akunaki.mcp_adapter` | **not present** |

---

## Documentation vs code

| Area | State |
|------|-------|
| `docs/` architecture set | Proposed design (mostly still forward-looking; Turso Cloud remains **future** context) |
| `backend/` | First real application code (this foundation) |
| Phase Zero overall | **In progress** — local libSQL foundation, atomic durable-job repository lifecycle, and single-process worker runtime tested; remote Turso intentionally deferred; encryption / volume / connector spikes open |

Evidence: [evidence/phase-zero-turso-foundation.md](evidence/phase-zero-turso-foundation.md), [evidence/phase-zero-job-concurrency.md](evidence/phase-zero-job-concurrency.md), [evidence/phase-zero-envelope-encryption.md](evidence/phase-zero-envelope-encryption.md).
