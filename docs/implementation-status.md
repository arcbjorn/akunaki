# Implementation status

**Last updated:** 2026-07-19

Honest truth table for what exists in this repository. Prefer this page over roadmap marketing language when asking “is it built?”

Legend:

- **Implemented** — code present in tree
- **Tested** — automated tests green on the documented platform
- **Pending** — designed in docs; not built or not proven

---

## Platform foundation (Phase Zero partial)

| Capability | Implemented | Tested | Pending / notes |
|------------|:-----------:|:------:|-----------------|
| Backend package layout (`src/akunaki/{domain,application,ports,adapters/db,api,worker}`) | yes | yes (import/boot) | layers mostly empty by design |
| Python 3.13.14 pin + `requires-python >=3.13.14,<3.14` | yes | yes | 3.14 rejected after segfault observation |
| Exact dependency pins + `uv.lock` | yes | yes (`uv lock --check`) | stable-only policy |
| pydantic-settings config (`AKUNAKI_` prefix) | yes | yes | no model config surface |
| Safe local default `sqlite+libsql` URL | yes | yes | parent dir created on engine build |
| Local-only `database_url` validation (official `sqlite+libsql://` memory / path memory / relative / absolute file) | yes | yes | rejects hostname, credentials, port, query, fragment, non-dialect |
| Remote Turso auth token / connect_args | **no** | n/a | **intentionally deferred** (not wired; not credential-blocked) |
| SQLAlchemy 2 engine/session + FK pragma + busy_timeout(50ms) + file WAL once | yes | yes | StaticPool in-memory; QueuePool file-backed (pool_size=5, max_overflow=5, pool_timeout=5) |
| Declarative base + naming conventions | yes | yes | |
| Database readiness probe | yes | yes | |
| Alembic env + migrations (through OIDC login states) | yes | yes (up/down/up through `0012`; legacy job + `system.noop` backfill; head derived, not hardcoded) | full product schema pending |
| Connection lifecycle schema (`connections`, `connection_secrets`, `connection_health`) | yes | yes | migration `0004`; one connection per provider per tenant; provider/status vocabularies; ciphertext-only token storage; cascade delete |
| libSQL-compatible `Blob` column type | yes | yes | driver exposes no DBAPI `Binary`, so stock `LargeBinary` cannot bind — see Turso evidence note 4 |
| ORM models agree with migration (columns/FKs/indexes) | yes | yes | IDs caller-supplied TEXT; no UUIDv7 helper |
| Domain job lifecycle/failure types + ports Protocol (model-free) | yes | yes | immutable failure results; no SQLAlchemy in domain/ports; second-precision times; min 1s lease TTL |
| JobRepository atomic execution lifecycle + leader fencing | yes | yes | claim creates one attempt; complete/fail/retry/dead-letter/expiry history are fenced local short transactions |
| Idempotent enqueue (`enqueue_job`) deduped on `(tenant_id, idempotency_key)` | yes | yes | atomic `INSERT ... ON CONFLICT DO NOTHING`; NULL key always inserts; tenant-scoped; duplicate job id without a key raises |
| Durable attempt history + one-to-one dead-letter records | yes | yes | retry scheduling uses exact caller-provided delay; redacted failure messages capped at 500 chars |
| Leader lease owner/expiry pair + nonempty name checks | yes | yes | migration `0002` + model agreement |
| FastAPI app factory + `python -m akunaki.api` | yes | yes | |
| `GET /healthz` (service, db ready, `models_required=false`) | yes | yes | does not fabricate product health |
| Internal debug surface (`/internal/debug/sync-status`) | yes | yes | **unauthenticated**; router not registered unless `AKUNAKI_DEBUG_ROUTES_ENABLED` is set (default off); `private, no-store`. `latest-sleep` **removed** — superseded by the authenticated `/v1/sleep`, so no unauthenticated route serves health values anymore |
| Security response headers + CORS | yes | yes | every response (incl. errors) carries a strict CSP (`default-src 'none'`), `nosniff`, `X-Frame-Options: DENY`, `no-referrer`, and same-origin COOP/CORP. Credentialed CORS only for the configured `cors_allowed_origins` allow-list (never `*`), methods GET/POST, headers `content-type`/`x-akunaki-csrf`; empty allow-list = no cross-origin browser access. Tested |
| Phase-one vertical slice (connect → sync → normalize → read in API) | yes | yes | verified end-to-end in a real process against a live HTTP provider |
| Authenticated `/v1` product surface | partial | partial | login, `/v1/session`, `/v1/sleep`, `/v1/recovery`, `/v1/today`, `POST /v1/checkin`, `/v1/tools`, `/v1/provenance/{token}`, and `/v1/connections/{provider}/authorize`+`/callback` reachable. The debug router remains a stand-in to be replaced |
| Tool registry (typed facade, AI-independent) | yes | yes | `ToolRegistry` + `Tool` with declared metadata (scopes, sensitivity, `side_effect`, `model_exposure`, `requires_confirmation`); Pydantic input/output validation; tenant from the tool **context**, never the input. Read-health tools `health.get_today`/`get_recovery`/`get_sleep` wrap the surface services with no formula. Exposed over `/v1/tools` (list + invoke) — the phase-two "**tools usable by REST without model packages**" exit criterion, proven end to end (CSRF-enforced invoke, 404/422 handling). Imports no model/AI SDK |
| `POST /v1/checkin` subjective check-in (first write path) | yes | yes | cookie-authenticated **and** CSRF-enforced (state-changing method), tenant from session; validates the three normalized inputs to [0, 1]; versioned write (re-submission supersedes). Verified end to end that a recorded check-in feeds the subjective factor into `/v1/recovery` |
| `GET /v1/sleep` deterministic sleep summary | yes | yes | authenticated, tenant from session; exact `sleep_summary_v0.1.0` adherence + 14-day debt; a **summary not a score** — no score field is exposed; unknown days disclosed as a lower bound; golden + end-to-end tests |
| `GET /v1/recovery` recovery surface | yes | yes | authenticated, tenant from session; runs the full assembled scoring path (`general_recovery_v0.1.0`) and returns score/status/confidence/coverage, present factors, and `data_gaps`. Recovery is the **only** shipping score code. A tenant **with** overnight HRV/RHR and a mature baseline gets a real 0-100 score with HRV/RHR factors; a sleep-only tenant is honestly `insufficient` disclosing `missing_hrv_or_resting_hr`. A fabricated-score regression is guarded at the HTTP boundary. End-to-end tested both ways |
| Score persistence (`daily_health_scores`, `score_factors`) | yes | yes | migration `0014`; versioned like facts (partial unique index on current `(tenant, day, score_code)`; supersede, never rewrite). `ScoreRepository.write_recovery_score` is idempotent by `dependency_hash` (identical recompute is a no-op) and persists signed factor rows (sign from the component's side of the 50 midpoint). Registry + `null-score-iff-insufficient` CHECKs enforced at the DB. `derivation_run_id` links a written score to its derivation run (migration `0020`) |
| `score.recompute` job handler | yes | yes | assembles the recovery surface for a local health day, persists it through the versioned score repository, and **detects + tracks anomalies** for the day; runs through the **real worker runtime**. A full-history day writes a real score with signed factors; a sleep-only day writes an honest `insufficient` row (a stored outcome, not an absence); redundant recompute is idempotent; a malformed payload dead-letters |
| Automatic recompute chaining (`raw.normalize` → `score.recompute`) | yes | yes | after writing facts, normalize enqueues one recompute per affected local health day, keyed `recompute:<revision_id>:<day>` so a normalize retry does not stack duplicates while a new revision (a correction) triggers a fresh recompute. Proven end to end through the real worker: sync → normalize → recompute → persisted score row |
| Derivation lineage + opaque provenance (`derivation_runs`, `derivation_inputs`, `GET /v1/provenance/{token}`) | yes | yes | migration `0020`; the `score.recompute` handler records a `derivation_run` (formula version, status, confidence, freshness) for each written recovery score and links it via `daily_health_scores.derivation_run_id`. The run mints an opaque `provenance_token` — a **public but unguessable** handle (stored in the clear, unique-constrained, `uq_derivation_token`), not a secret — that `/v1/today` returns as `provenance_url`. `GET /v1/provenance/{token}` is authenticated and **tenant-scoped**: it resolves the token to disclosed lineage (kind, versions, status, freshness, input **roles**) and never leaks a table, raw, or run id; an unknown token and a cross-tenant token are the same 404 (a token cannot be probed). Typed fact-id inputs are **not threaded yet** (the input service exposes roles, not fact ids) so runs carry no per-input rows for now. Repository + route + recompute-traceability tested end to end |
| Serve stored scores on read (`/v1/recovery`, `/v1/today`) | yes | yes | `ServedRecoveryService` returns the persisted current score (with its factors, and gaps re-derived from present factor codes via the shared pure rule), falling back to compute-on-read only for a day never scored. Both surfaces read from storage; the compute path stays for the recompute handler. The served `version_n` and `freshness_at` are disclosed in the response body (null when computed on read); `/v1/today` carries the recovery score's `freshness_at` at top level. Tested via a fake reader (branching) and end to end (a seeded score served verbatim, computed-on-read has null metadata) |
| `GET /v1/today` composite surface | yes | yes | authenticated, tenant from session; stitches the recovery score, the sleep summary, the **deterministic training label**, and the **primary/supporting recommendations**, mirroring the recovery status at top level. Strain and activity **do not ship** in v0.1.0 — absent from the body and disclosed as `*_not_available` gaps, never fabricated; a no-sleep day omits the sleep block (no phantom zero-duration measurement) and gaps are deduplicated. The training label now applies the persisted-anomaly floor, the day's **descriptive ACWR** over-load downshift (from workout load), and the check-in **symptom-burden** floor. Carries the served score's opaque `provenance_url` (null when computed on read, no persisted run). End-to-end tested |
| Worker entry `python -m akunaki.worker` runtime | yes | yes | claim loop + SIGINT/SIGTERM cooperative shutdown; JSON logs |
| Worker runtime (claim → execute → heartbeat → settle) | yes | yes | port-typed in `application`; fake-repository policy tests + file-backed end-to-end tests |
| Retry classification + exponential backoff policy | yes | yes | transient/permanent/cancelled; capped jitter; min 1s (second-precision lifecycle) |
| Handler registry (`system.noop` built in) | yes | yes | unregistered `job_type` dead-letters instead of burning attempts |
| Background lease heartbeat | yes | yes | daemon thread; lease loss suppresses completion (no false success) |
| Leader-gated reaper tick (requeue expired / dead-letter exhausted) | yes | yes | standby never reaps without the `core-reaper` leader lease |
| Leader-gated periodic scheduler | yes | partial | pure `due_schedules` decides which `ScheduleSpec`s are due (never-fired → due immediately; else interval-elapsed); the worker fires them **only under the `core-reaper` leader lease**, on the reaper tick, tracking last-fired in memory, so at most one worker schedules. Each fire is **idempotency-keyed** so a lost lease or crash mid-fire never duplicates. Fake-repo worker tests + pure decision tests. **Production wiring pending**: the reconcile-sweep schedule is not yet registered in `python -m akunaki.worker` because a system-wide job needs a system-tenant convention (the `jobs.tenant_id` FK is non-null) — the mechanism is complete and general, the entrypoint wiring awaits that |
| Core-only / no model SDKs | yes | yes | import-linter + tests |
| Ruff / mypy / import-linter / pytest / pip-audit gates | yes | yes | see evidence docs |
| CI workflow (GitHub Actions) | yes | yes | four jobs: quality, migrations up/down/up on an ephemeral DB, core-only boot with models disabled, advisory `pip-audit`; every step verified locally before commit |
| Core-only boot proven without dev deps | yes | yes | CI installs with `--no-dev` and asserts no model SDK is importable, then boots API + worker with no `MODEL_*` config |
| Frontend / web | no | no | deferred |
| Session layer (`users`, `sessions`: issue, validate, rotate, revoke, purge) | yes | yes | migration `0011`; **hash-only** storage of cookie token and CSRF secret; expiry + revocation enforced; rotation revokes the predecessor |
| OIDC login state (`login_states`: hashed `state` + `nonce`, sealed PKCE verifier) | yes | yes | migration `0012`; separate from `oauth_states` because login has **no tenant yet** and needs a nonce; single-use, expiring, exact redirect match |
| `id_token` claim validation (iss, aud, nonce, exp/nbf/iat, sub) | yes | yes | pure and clock-injected; 60s skew tolerance; rejected tokens never yield an identity; email redacted in `repr` |
| OIDC client (discovery, authorize URL, token exchange, JWKS signature verification) | yes | yes | PyJWT against the issuer's JWKS; **asymmetric algs only** (HS256 refused — alg-confusion closed); real-HTTP verified; secrets never logged |
| OIDC login flow (`/auth/login`, `/auth/callback`, user upsert, session issue) | yes | yes | port-typed service; state sealed before redirect, single-use consume, verify-before-session; first login provisions tenant+user; end-to-end verified against a real HTTP IdP through to an authenticated `/v1` request |
| User provisioning (one user per tenant, MVP) | yes | yes | keyed by `(oidc_issuer, oidc_subject)`; first login provisions tenant + user atomically; email refreshed but never used as identity |
| Session cookie wiring + CSRF enforcement | yes | yes | `Secure`/`HttpOnly`/`SameSite=Lax`; CSRF required on POST/PUT/PATCH/DELETE; one generic 401 so unknown/expired/revoked are indistinguishable |
| `GET /v1/session`, `POST /v1/session/logout`, `POST /v1/session/logout-everywhere` | yes | yes | tenant comes from the validated session, never a request parameter; logout revokes server-side **and** clears the cookie; logout-everywhere revokes every live session for the user (stolen-device / password-change) leaving other users untouched, CSRF-enforced |
| Connectors (Oura, Google Health, Polar) | partial | partial | Oura OAuth, fetch, initial sync, and sleep normalization ship. **Polar** fetch client, workout normalizer, and now the **provider-parameterized backfill** (`sync_config_for_provider`) ship — the same initial-sync handler backfills a linked Polar connection into `workout_sessions`, proven end to end; Polar **OAuth token exchange** (`PolarOAuthClient`) ships. **Google Health** now has its v4 fetch client, sleep-segment normalizer, backfill config, and **OAuth client** (`GoogleHealthOAuthClient`: PKCE + `access_type=offline`/`prompt=consent` for a refresh token, form-body client_secret) — the same handler backfills a Google Health connection into `sleep_sessions`, proven end to end. The **linking service now handles all three providers** (PKCE and non-PKCE via `uses_pkce`), and the **authenticated HTTP link routes** (`/v1/connections/{provider}/authorize`+`/callback`) drive a full browser link end to end. **Incremental sync** ships too — `connection.incremental_sync` resumes from the stored cursor (minus overlap) instead of a full lookback, degrading to a lookback on the first run. **Webhooks** ship for **all three providers**: Oura/Polar verify an HMAC-SHA256 body signature, and Google Health verifies its Google-signed push OIDC token (rotating JWKS keys + audience/service-account claims). A verified delivery is recorded once in a deduplicated inbox and triggers an incremental-sync refetch. The full connector lifecycle (link → backfill → webhook/incremental sync → normalize → source-select → score) is now complete for all three |
| Agent / model packages | no | no | forbidden in core |
| Full data-model schema | no | no | tenants, durable-job lifecycle, connection lifecycle, OAuth state, sync transport, and the **sleep** fact slice exist; a **deterministic sleep source-precedence** ships and is now **persisted** as versioned `source_selections` + candidates (daily-metric slice, migration `0023`); the session/workout `source_grains`/`source_grain_versions`/`source_grain_members` grain tables remain pending (deferred until cross-provider episode matching), along with the `source_policies` rules tables (policy is code-defined for now) |
| Envelope encryption (AES-256-GCM, KEK/DEK, rotation, AAD binding) | yes | yes | fresh DEK+nonces per seal; versioned KEK registry; fail-fast boot without keys; mutation-checked randomness |
| Sealed tokens persisted to `connection_secrets` | yes | yes | raw column holds no readable token; envelope bound to its connection; cascade delete |
| KEK sourcing from external KMS / secret manager | no | no | keys load from `AKUNAKI_SECRET_KEKS` only; no KMS client, rotation runbook, or key-use audit |
| OAuth state / PKCE handshake primitives (`oauth_states`) | yes | yes | migration `0005`; hashed state only, sealed PKCE verifier, exact redirect match, single-use + expiry, purge sweep |
| Oura OAuth client (authorize URL, PKCE code exchange, refresh) | yes | yes | S256 only; typed failure vocabulary (`invalid_grant` → reauth vs retryable); secrets never logged; mock-transport + real-HTTP verified |
| OAuth linking service (start link → callback → sealed tokens) | yes | yes | port-typed in `application`; `tenant_id` is a parameter; connection row + sealed secret written in **one** transaction |
| Connection repository (link/relink, status transitions) | yes | yes | relink reuses the existing `(tenant_id, provider)` row; failed exchange leaves no half-written connection |
| Sync transport schema (`sync_runs`, `raw_payload`, `sync_cursors`, `raw_objects`, `raw_revisions`) | yes | yes | migration `0006`; every response retained (hash indexed, **not** unique); append-only revisions; `superseded` rejected as a tombstone reason |
| `webhook_inbox` table + webhook verification | yes | yes | migration `0021`; `webhook_inbox` durable deduplicated deliveries, unique on `(connection_id, dedupe_key)`, with the sole one-way FK `body_payload_id` → `raw_payload` (nullable, `SET NULL`) and a `json_valid` header-meta CHECK. Pure `webhook_verification` does **constant-time HMAC-SHA256** over the exact body (Oura/Polar). **Google Health** verifies its Google-signed push OIDC token: `GooglePushVerifier` checks the Bearer JWT's signature against Google's rotating **JWKS** (PyJWT, asymmetric algorithms only — HS256 downgrade rejected), then the pure `validate_google_push_claims` checks issuer, audience, expiry, and the push **service-account** email (`email_verified`). `WebhookInboxRepository.record_delivery` is an atomic `INSERT ... ON CONFLICT DO NOTHING` returning duplicate-or-new. `POST /webhooks/{provider}/{connection_id}` (unauthenticated — vendor-called, trust from the signature/token) verifies, records, acknowledges fast, and enqueues a `connection.incremental_sync` refetch (idempotency-keyed). A bad signature/token, tampered body, wrong service account, or unknown connection is an indistinguishable 401; an unconfigured provider a 404; a redelivery is deduped (no second job). The webhook only **triggers** a pull — it never trusts the delivered body as data. Verified end to end over HTTP for all three providers |
| Oura V2 fetch client (windowed pages, pagination, typed failures) | yes | yes | exact body returned for faithful transport persistence; 401/403 → `unauthorized`, 429 → `rate_limit` with `Retry-After`; token never logged |
| Atomic fetch commit (`IngestionRepository`) | yes | yes | transport row + logical revision + cursor in **one** transaction; revision appended only on new `content_hash` |
| `connection.initial_sync` handler | yes | yes | opens sealed tokens, paginates, translates fetch outcomes into the worker's retry vocabulary; auth failure → `needs_reauth` + dead letter, 429/5xx → retry |
| `connection.incremental_sync` handler | yes | yes | reuses the initial-sync page loop (extracted to a shared `sync_window`) but sets the window start from the **stored cursor minus overlap** rather than a full lookback, so only new data is pulled; the overlap re-fetches recently-finalized records (content-hash dedupe makes an unchanged re-fetch a no-op). First run (no cursor) degrades to the lookback backfill; a malformed cursor falls back to the lookback rather than wedging the connection; a cursor older than the lookback horizon is clamped to it. Verified against a real DB + worker: first-run window, cursor resume, malformed-cursor fallback, stale-cursor clamp |
| `connection.reconcile_sweep` handler | yes | yes | scheduled reconciliation that catches gaps a missed webhook or sync left behind: finds **active** connections whose last successful sync (`connection_health.last_success_at`, now written on every ACTIVE transition) is older than a staleness cutoff (default 6h) or never synced, and enqueues an incremental sync for each — **idempotency-keyed per connection** (`reconcile:<id>`) so a connection already queued by a webhook or an earlier sweep is not double-scheduled. `needs_reauth`/`error` connections are skipped (they cannot sync until re-consent). Real-DB stale query + fake-port handler tested |
| Vendor record identity in the **raw** layer | yes | yes | per-record: one page yields one `raw_object`/`raw_revision` **per record**, keyed `stream:<vendor_id>` (or a body hash when the vendor supplies none); each revision stores its own slice body (`slice_json`) |
| Stable ids for streams without a vendor id | partial | yes | falls back to hashing the record body — per-record, but a cosmetic vendor change re-identifies the record; only `sleep`/`daily_*`/`workout` have mapped id fields |
| Sleep fact schema (`fact_records`, `sleep_sessions`) | yes | yes | migration `0007`; versioned headers with a partial unique index on current; typed one-to-one detail (not EAV) |
| Oura sleep normalizer (pure, deterministic) | yes | yes | wake-date assignment, canonical minutes, honest quality grading; no clock, so re-runs are byte-identical |
| Versioned fact writes (supersede, never update in place) | yes | yes | identical content is a no-op; changed content appends a version and retains the prior row with its detail |
| Rolling baseline + robust z-score (`general_recovery_v0.1.0`) | partial | partial | pure domain: 42-day prior window, median center, MAD→robust_scale with the IQR fallback and per-metric floors, maturity gate (insufficient/min/mature at 14/28), z clamped to [-3, 3]; **no imputation** — missing days are absent, not zero; golden + mutation tested. **Takes the present sample series** — windowing/stratification from facts, and wiring z into recovery components, are the next units |
| Recovery composite engine (`general_recovery_v0.1.0`) | partial | partial | pure domain: exact component weights, baseline z-score→c curve, bounded adherence, piecewise freshness, the three-part sufficiency gate, weighted mean renormalized over **present** weights with disclosed coverage, and `confidence = coverage*freshness*quality*baseline_maturity`; golden + mutation tested. Takes already-mapped component scores; the baseline/ACWR/check-in mapping, persistence, and the `/v1` surface are all wired now (ACWR also feeds the training-label downshift on `/v1/today`) |
| Feature→component bridge | partial | partial | pure seam composing baseline z-score → directed `c` for baseline-dependent components and direct pass-through for sleep adherence; an insufficient baseline **omits** the component (returns None), never a midpoint. Verified end to end that **sleep-only data is honestly `insufficient`** (gate 2 needs HRV or RHR, which no fact table sources yet). **Windowing/stratification from stored facts** and the per-component feature queries are the next units |
| Windowed feature queries + recovery-input assembly | yes | yes | `daily_sleep_efficiency`, `daily_hrv`, `daily_resting_hr`, `daily_temperature_deviation`, `daily_respiratory_rate`, and `daily_principal_sleep_midpoint` windowed queries (each omits days where the value is unknown), the 42-day `baseline_window_days` helper (target day excluded), and `RecoveryInputService` that builds every present component (adherence + consistency direct; efficiency/HRV/RHR/temperature/respiratory each present only with a mature baseline). Verified against a real DB that a tenant **with** overnight HRV/RHR and a mature baseline reaches a real score, while a sleep-only tenant stays `insufficient`. Prior-load/subjective still await their sources |
| Sleep consistency (mean resultant length) | yes | yes | pure `sleep_consistency`: principal-sleep local-time midpoints on the `[0, 1440)` circle over the 14-day window, `R = sqrt(mean(cos)² + mean(sin)²)`, score `100·R`; **direct 0-100, no baseline**; requires ≥7 valid nights or the component is omitted. Golden + mutation tested; verified end to end that regular sleep scores 100, <7 nights omits, and naps are not valid nights |
| Prior-load balance (descriptive ACWR) | yes | yes | pure `prior_load`: ACWR = acute (7-day) / chronic-weekly (28-day / 4) under **strict** coverage (any unknown day → undefined → component omitted); the exact band mapping (under-load toward 0, 0.8-1.3 balance band = 100, over-load to 0 at a≥2.0); the `all_zero_rest` defined-balanced case and zero-chronic/nonzero-acute undefined case. Golden + mutation tested. **Now sourced from workout facts** — `daily_strain_load` sums included session loads; verified against a real DB that full 28-day workout coverage activates the component (ACWR defined) and a coverage gap omits it |
| Canonical zone-load + Polar workout normalizer | yes | yes | pure `zone_load` (`session_load = Σ minutes_z · weight_z`, default Z1..Z5 = 1..5; daily strain-load = sum of included sessions) — canonical load computed **internally**, never a vendor field. Pure `polar_workout_v0.1.0` normalizer parses HR-zone durations (ISO-8601 or seconds) into `workout_sessions` (migration `0019`), local-start-date-keyed, incomplete-zone records skipped. Flows through the real ingestion loop via schema-version dispatch (`polar.*` → workouts). Golden + mutation tested |
| Polar fetch client (AccessLink) | yes | yes | `PolarFetchClient` fetches the `exercises` list with Bearer auth; returns the **exact** body for faithful retention (token never logged or in `request_meta`), classifies 401/429/5xx/malformed to typed failures. Mock-transport tested (no network). **Not yet wired to a Polar-provider sync config/handler** — the fetch client + normalizer both exist; a `SyncConfig(schema_version="polar.v1", stream="workout")` path and Polar OAuth token exchange would complete a live pull |
| Subjective modifier (completed check-in) | yes | yes | pure `subjective`: `c = clamp(100·(0.5·energy_n + 0.25·(1−stress_n) + 0.25·(1−symptom_burden_n)), 0, 100)`; present **only** when all three normalized fields of a completed check-in are known (a missing check-in or blank field omits it, never a neutral 50). Golden + mutation tested. Sourced from `subjective_check_ins` (migration `0017`, versioned) via `POST /v1/checkin`. **This completes all 9 recovery component families** |
| Anomaly detectors + persistence (Stage 4) | yes | yes | pure `anomalies`: the six enabled v0.1.0 detectors (exact z-thresholds, moderate/high at the clamp), direction-aware clear checks, and the **2-consecutive-day clear state machine** (open/update/close with peak-severity retention). Persisted via the `anomalies` interval table (migration `0018`, one active per feature) through `AnomalyRepository` + `AnomalyTracker`. **The `score.recompute` handler now detects and tracks anomalies automatically**: `RecoveryInputService.feature_signals` computes each feature's z (the same z its component used) and the tracker advances the interval — verified end to end that a far-below-baseline HRV opens a `low_hrv` interval during recompute. A persisted high-severity anomaly floors the training label at `light` on `/v1/today`. Golden + mutation tested. **The low-activity anomaly is now live too** — daily steps ingest (see the activity ingestion row) feed `feature_signals`, and a far-below-baseline steps day opens a `low_activity` interval. All six v0.1.0 detectors are now sourced |
| Training label + recommendations (Stage 5) | yes | yes | pure `training_label` (`training_label_v0.1.0`: recovery-band base + ordered downshift; missing data never rests, high-severity anomaly floors at light) and `recommendations` (the exact rule predicates + priority/conflict-group resolution to one primary). Golden + mutation tested. Wired into `/v1/today`: `training_recommendation` + `primary_recommendation`/`supporting_recommendations` ship (verified end to end). **Both the anomaly and ACWR downshift/rule paths are now threaded**: a persisted high-severity anomaly floors the label at light, and the day's descriptive ACWR (from `RecoveryInputService.acwr_for_day`, the same load windows the prior-load component reads) drives the over-load downshift — proven at the composite that ACWR > 1.3 caps a HARD day at MODERATE while an undefined ratio leaves it untouched. The **symptom burden** is threaded too (from `RecoveryInputService.symptom_burden_for_day`, the same completed check-in the subjective component reads): a burden ≥ 0.75 floors a HARD day at LIGHT, an absent check-in leaves it untouched. The `severe_symptom_flag` REST trigger stays dormant — the v0.1.0 check-in has no distinct severe-symptom marker |
| Overnight vitals (HRV, RHR, temperature, respiratory) detail table + normalizer | yes | yes | migrations `0013`/`0015`/`0016`; `overnight_vitals` one-to-one detail (at-least-one-of-four invariant); pure `oura_vitals_v0.1.0` normalizer extracts `average_hrv`/`lowest_heart_rate`/`temperature_deviation` (flat or nested `readiness`)/`average_breath` from the sleep payload, wake-date-keyed like sleep, naps and empty-signal records skipped, sanity-bounded; flows through the real ingestion loop with shared lineage. **HRV/RHR is what lets the recovery gate pass**; temperature (0.10) and respiratory (0.05) add components |
| Daily activity (steps, active minutes) detail table + normalizer | yes | yes | migration `0022`; `daily_activity` one-to-one detail (steps INTEGER, active_minutes; at-least-one-signal + non-negative CHECKs). Pure `google_activity_v0.1.0` normalizer reads Google Health daily-activity data points, local-date-keyed, sanity-bounded, empty-signal days dropped. Flows through the ingestion loop via the `google_activity.` schema-version dispatch into `daily_activity`. `FactRepository.daily_activity_steps` feeds `feature_signals`, so a far-below-baseline steps day opens the `low_activity` anomaly (golden + real-DB tested). The activity **score** stays blocked (no accepted formula) |
| Sleep source selection (multi-provider precedence) | partial | yes | pure `source_policy_v0.1.0`: a deterministic sleep-provider precedence (`authoritative_sleep_provider`) — **Oura wins any day it covers, Google Health is the fallback**, no averaging and no cross-provider fallback. Applied in the three sleep feature queries (`daily_sleep_durations`/`daily_sleep_efficiency`/`daily_principal_sleep_midpoint`): when two providers cover a night the repo reads only the authoritative provider's sessions, fixing a **double-count** the second sleep connector would otherwise cause. Verified against a real DB (Oura 420 chosen over a 820 sum; efficiency never blended; a longer Google session is not the principal one). The decision is now also **persisted and auditable**: `source_selections` (daily-metric slice, migration `0023`) records one **current** versioned decision per `(tenant, metric_family, day)` with the competing providers as `source_selection_candidates` (the "Why"); a changed decision supersedes (one-current partial unique index), `missing_authoritative` carries no fact + a required reason, and an identical decision is idempotent — real-DB tested. Still the daily-metric slice **only**: the session/workout `source_grains`/`source_grain_versions`/`source_grain_members` machinery is deferred until cross-provider episode matching is needed (one workout provider today) |
| Directed z-mapping modes | yes | yes | `Direction` enum with the four exact v0.1.0 forms: `+z` (HRV), `-z` (RHR), `-\|z\|` (temperature: any deviation worse), `-max(z,0)` (respiratory: elevated worse). Golden + mutation tested; all four now sourced |
| Other detail tables (labs, …) | no | no | **deliberately deferred**: each arrives with the normalizer that populates it, not as empty tables. Sleep, overnight vitals, workouts, and daily activity now ship |
| `raw.normalize` job handler | yes | yes | keyed by `raw_revision_id`; enqueued in the **same transaction** as the revision, so a revision can never exist without its normalize job |
| Closed ingestion loop (sync → normalize → facts) | yes | yes | drained through the real worker; verified end-to-end against a live HTTP server with lineage intact |
| Connector OAuth HTTP routes (authorize/callback) | yes | yes | `GET /v1/connections/{provider}/authorize` + `/callback` — **authenticated** (tenant from the session, never a parameter), wiring the provider-uniform linking service. Per-provider OAuth credentials come from config (`{provider}_client_id`/`_client_secret`/`_redirect_uri`); a provider not fully configured, or unknown, is an indistinguishable **404** (no half-built connect surface). `build_oauth_client` maps a provider to its concrete client; a transient exchange failure is 503, a permanent one 400. Verified end to end over HTTP: authorize → captured state → callback links an `active` connection (Polar, non-PKCE), unknown/unconfigured provider → 404, no session → 401 |
| Polar / Google Health fetch clients | partial | partial | **Polar** `PolarFetchClient` (AccessLink exercises) ships, is normalizer-wired, and has its **backfill config** (`sync_config_for_provider("polar")` → `workout`/`polar.v1`); the initial-sync handler backfills a linked Polar connection end to end (fetch → `polar.v1` revisions → `workout_sessions`). The **Polar OAuth client** (`PolarOAuthClient`: authorize URL + Basic-auth code exchange, no PKCE, no refresh token, captures `x_user_id` as the connection's `external_user_id`) now ships. The **linking service now links a non-PKCE provider** (Polar) end to end — a `uses_pkce` capability on the client port makes `start_link`/`complete_link` thread a verifier only for PKCE providers, sealing an empty placeholder otherwise (the state's CSRF single-use protection is identical); the connector link routes now ship (see the connector OAuth HTTP routes row), so a browser can link Polar end to end. **Google Health** now ships its **v4 fetch client** (`GoogleHealthFetchClient`: `dataPoints:reconcile` POST, RFC3339 window, `nextPageToken` pagination — sleep stream) and a pure **sleep-segment normalizer** (`google_sleep_v0.1.0`: aggregates stage segments into one canonical session per wake-date, shared `SleepFact` output). Wired into the backfill (`sync_config_for_provider("google_health")` → `sleep`/`google_health.v4`) and normalize dispatch — a linked Google Health connection backfills into `sleep_sessions` end to end. The **Google Health OAuth client** (`GoogleHealthOAuthClient`: authorize URL with PKCE S256 + `access_type=offline`/`prompt=consent`, form-body client_secret code exchange, refresh) now ships too. With the linking service now provider-uniform and the link routes mounted, a browser can drive a full Google Health link |
| Concurrent worker runtimes (exactly-once execution, single leader, stolen-lease safety) | yes | yes | bounded local stress: 3 workers/24 jobs, 4 contending reapers, independent engines |
| Sustained multi-process fleet under production load | no | no | in-process threads with independent engines only; no long-running or cross-host soak |
| Product job handlers | yes | yes | `connection.initial_sync`, `connection.incremental_sync`, `connection.reconcile_sweep`, `raw.normalize`, and `score.recompute` ship, chained: sync enqueues normalize (same commit as the revision), normalize enqueues recompute per affected day; the reconcile sweep fills sync gaps a missed webhook would leave |
| Atomic domain side-effect unit of work | no | no | lease validity primitive exists; fenced side-effect UoW still pending |
| Remote production Turso (Turso Cloud) | no | no | **product deferred**; proposed in ADR 0003 only |
| Privacy delete stub (cancel jobs → scrub rows → completion proof) | yes | yes | phase-one exit criterion; ordering enforced (scrub before cancel is rejected); tenant-scoped; proof carries counts only |
| Restoration-suppression ledger | no | no | **deliberately not built**: needs a dedicated deletion key with access separation; an empty table would imply a guarantee the system cannot make |
| Backup expiry on deletion | no | no | pipeline records the stage; no backup provider is wired, so nothing is actually expired |
| Multi-tenant fact isolation | yes | yes | fact identity is tenant-scoped (migration `0010`); regression-tested after a collision bug where one tenant's fact silently overwrote another's |
| Encryption-at-rest / backup evidence | partial | partial | application-level envelope for secret columns done (see [evidence](evidence/phase-zero-envelope-encryption.md)); platform at-rest, backup/export encryption, and key separation runbooks open |
| Volume / vector spikes | no | no | Phase Zero open |

---

## Process entrypoints

| Entrypoint | Behavior today |
|------------|----------------|
| `python -m akunaki.api` | Serves core API; `/healthz` only |
| `python -m akunaki.worker` | Boots config/DB, readiness, then runs the durable claim loop until SIGINT/SIGTERM |
| `python -m akunaki.agent_worker` | **not present** |
| `python -m akunaki.mcp_adapter` | **not present** |

---

## Documentation vs code

| Area | State |
|------|-------|
| `docs/` architecture set | Proposed design (mostly still forward-looking; Turso Cloud remains **future** context) |
| `backend/` | First real application code (this foundation) |
| Phase Zero overall | **In progress** — local libSQL foundation, atomic durable-job repository lifecycle, and single-process worker runtime tested; remote Turso intentionally deferred; encryption / volume / connector spikes open |

Evidence: [evidence/phase-zero-turso-foundation.md](evidence/phase-zero-turso-foundation.md), [evidence/phase-zero-job-concurrency.md](evidence/phase-zero-job-concurrency.md), [evidence/phase-zero-envelope-encryption.md](evidence/phase-zero-envelope-encryption.md).
